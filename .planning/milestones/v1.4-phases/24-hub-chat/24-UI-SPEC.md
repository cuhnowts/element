---
phase: 24
slug: hub-chat
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-04-01
revised: 2026-04-01
---

# Phase 24 — UI Design Contract

> Visual and interaction contract for the Hub Chat phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | base-nova |
| Component library | radix |
| Icon library | lucide |
| Font | Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding within message bubbles |
| sm | 8px | Gap between avatar and bubble, chip vertical padding |
| md | 16px | Message vertical spacing, input padding, suggestion chip horizontal padding |
| lg | 24px | Section padding between briefing and chat area |
| xl | 32px | Chat container horizontal padding |
| 2xl | 48px | Not used this phase |
| 3xl | 64px | Not used this phase |

No exceptions declared.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Label | 12px | 400 (regular) | 1.4 |
| Heading | 16px | 600 (semibold) | 1.3 |
| Code (inline) | 12px | 400 (regular) | 1.5 |

Weights used: 2 (400 regular, 600 semibold).

Notes:
- Body (14px/400) is the project-wide default from `app.css`. All chat message text uses this.
- Label (12px/400) for timestamps, "Stop generating" button text, suggestion chip labels, and code block language labels. Visual distinction from Body comes from size difference (12px vs 14px), not weight.
- Heading (16px/600) for rendered markdown H1-H3 inside bot responses (H1=16px, H2=15px, H3=14px semibold -- kept compact to avoid overwhelming the chat scroll).
- Code uses the terminal monospace stack: `'SF Mono', 'Fira Code', 'Cascadia Code', Menlo, monospace` at 12px, matching Label size for consistency. The monospace font itself provides sufficient visual distinction from Body text.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--color-background)` oklch(0.145 0 0) | Chat scroll area background, input area background |
| Secondary (30%) | `var(--color-card)` oklch(0.205 0 0) | Bot message bubbles, suggestion chips, input field background |
| Accent (10%) | `var(--color-primary)` oklch(0.985 0 0) | Send button, user message bubble background, streaming cursor |
| Destructive | `var(--color-destructive)` oklch(0.396 0.141 25.723) | Error state text, failed message indicator |

Accent reserved for:
- Send button (icon fill)
- User message bubble background (with `var(--color-primary-foreground)` text)
- Streaming cursor/caret indicator
- Active suggestion chip hover state

Additional semantic colors:
- User bubble text: `var(--color-primary-foreground)` oklch(0.205 0 0) -- dark text on light accent bubble
- Bot bubble text: `var(--color-card-foreground)` oklch(0.985 0 0) -- light text on dark card bubble
- Muted text: `var(--color-muted-foreground)` oklch(0.708 0 0) -- timestamps, placeholder text
- Border: `var(--color-border)` oklch(0.269 0 0) -- input field border, divider between briefing and chat

---

## Visual Hierarchy

Primary focal point: chat input area (pinned bottom), which anchors the user's attention as the action entry point.

---

## Component Inventory

Components needed for this phase, mapped to source:

| Component | Source | Notes |
|-----------|--------|-------|
| ScrollArea | shadcn (existing) | Chat message scroll container |
| Button | shadcn (existing) | Send button, stop button, suggestion chips |
| Textarea | shadcn (existing) | Auto-expanding chat input (base element) |
| Skeleton | shadcn (existing) | Streaming placeholder shimmer |
| Separator | shadcn (existing) | Divider between briefing section and chat |
| ChatMessage | custom | Bubble layout component (user vs bot styling) |
| ChatInput | custom | Auto-expanding textarea with send/stop controls |
| SuggestionChips | custom | Contextual prompt suggestions, disappear after first message |
| MarkdownRenderer | custom | react-markdown wrapper with remark-gfm + rehype-highlight |
| StreamingCursor | custom | Blinking caret appended to streaming bot response |

---

## Interaction Contracts

### Chat Input (D-03)

- Auto-expanding textarea: starts at 1 line (40px height), grows to max 4 lines (~120px).
- Enter key sends the message. Shift+Enter inserts a newline.
- Input is disabled while a response is streaming (send button becomes stop button).
- Placeholder text: "Ask about your projects..." (muted foreground).
- Send button: right-aligned inside the input area, `SendHorizontal` icon from Lucide, 32x32px touch target.
- Send button is disabled (opacity 0.5) when input is empty.

### Message Display (D-01, D-02)

- User messages: right-aligned, accent background (`var(--color-primary)`), dark text, max-width 80% of container, border-radius 12px (top-left, top-right, bottom-left) with 4px bottom-right for the "tail" corner.
- Bot messages: left-aligned, card background (`var(--color-card)`), light text, max-width 85% of container, border-radius 12px (top-left, top-right, bottom-right) with 4px bottom-left for the "tail" corner.
- Messages appear in chronological order, newest at bottom.
- Container auto-scrolls to bottom on new message. If user has scrolled up, a "scroll to bottom" pill appears.
- 16px (md) vertical gap between consecutive messages.
- 8px (sm) vertical gap between consecutive messages from the same sender.

### Suggestion Chips (D-04)

- 3-4 horizontally-wrapped pill buttons below the briefing, above the chat input.
- Card background with border, md (16px) horizontal padding, sm (8px) vertical padding, border-radius 9999px (full pill).
- On hover: slightly lighter background (accent-foreground at 10% opacity overlay).
- On click: chip text populates the input and auto-sends.
- Chips are context-aware (generated from manifest, not hardcoded).
- Chips disappear permanently after the first user message in the session.

### Streaming (D-05, D-07)

- Bot response streams token-by-token. Markdown renders progressively as tokens arrive.
- A blinking cursor (vertical bar, 2px wide, accent color, 1s blink interval) appends to the end of the streaming text.
- "Stop generating" button replaces the send button during streaming. Uses `Square` Lucide icon (stop icon).
- On stop: partial response is kept in the chat. Cursor disappears. Input re-enables.
- If streaming errors: display inline error below the partial response (not a toast). Red muted text.

### Markdown Rendering (D-06)

- Full GFM support: headings, bold, italic, strikethrough, bullet/numbered lists, blockquotes, tables, links, images.
- Code blocks: dark background (`oklch(0.12 0 0)`), 1px border, sm (8px) padding, border-radius 6px. Syntax highlighting via rehype-highlight. Language label in top-right corner (12px, muted foreground).
- Inline code: slightly darker background than bubble, 2px horizontal padding, border-radius 3px.
- Links: accent-colored, underline on hover. Open in external browser via Tauri shell.open.
- Tables: full-width within bubble, border-collapse, alternating row backgrounds.
- Paragraphs within bot messages: sm (8px) bottom margin between paragraphs.

### Scroll Behavior

- Chat area fills available vertical space between briefing/separator and input.
- ScrollArea component with vertical scrollbar (auto-hide).
- On new message or streaming update: auto-scroll to bottom UNLESS user has scrolled up more than 100px from bottom.
- "Scroll to bottom" floating pill: appears when user is scrolled up, positioned sm (8px) above the input area, centered horizontally. Card background, "ChevronDown" Lucide icon, border-radius 9999px. Disappears on click or when user scrolls to bottom naturally.

### Empty State (before first message, after briefing)

- Suggestion chips are the empty state. No additional empty state needed since the briefing occupies the top of the center column.

### Error States

- **Provider not configured:** Inline message in chat area: "No AI provider configured. Set one up in Settings > AI to start chatting." with a "Open Settings" link/button.
- **Network/API error:** Inline error below the failed bot message: "Response failed -- check your connection and try again." with a "Retry" button that resends the last user message.
- **Rate limit:** Inline error: "Rate limited -- wait a moment and try again."
- All error messages use `var(--color-destructive-foreground)` text color.

### Agent Panel Boundary (D-12, D-13, D-14)

- Hub chat does NOT open or trigger the agent panel.
- Agent toggle button remains visible in sidebar per existing behavior.
- No shared state between useHubChatStore and useAgentStore.
- Agent notifications do not appear in the chat stream.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Send message" (aria-label on send button; button itself is icon-only) |
| Input placeholder | "Ask about your projects..." |
| Stop button label | "Stop generating" (aria-label; button itself is icon-only) |
| Scroll-to-bottom label | "Scroll to latest" (aria-label; button is icon-only) |
| Empty state heading | N/A (suggestion chips serve as empty state) |
| Empty state body | N/A (briefing occupies top; chips prompt action) |
| Error: no provider | "No AI provider configured. Set one up in Settings > AI to start chatting." |
| Error: network failure | "Response failed -- check your connection and try again." |
| Error: rate limit | "Rate limited -- wait a moment and try again." |
| Suggestion chip examples | "What should I focus on today?", "Summarize current progress", "Any blockers across projects?", "What's overdue?" |
| Destructive confirmation | N/A (no destructive actions in this phase; Phase 25 handles action dispatch) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | ScrollArea, Button, Textarea, Skeleton, Separator | not required |

No third-party registries declared. `registries: {}` in components.json confirmed empty.

---

## New Dependencies

| Package | Purpose | Justification |
|---------|---------|---------------|
| react-markdown | Markdown rendering in bot responses | D-06 requires full markdown support; no markdown library currently installed |
| remark-gfm | GFM extensions (tables, strikethrough, task lists) | D-06 specifies tables and full markdown |
| rehype-highlight | Syntax highlighting in code blocks | D-06 specifies code blocks with syntax highlighting |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
