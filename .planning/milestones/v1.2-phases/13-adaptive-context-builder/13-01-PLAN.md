---
phase: 13-adaptive-context-builder
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/models/onboarding.rs
  - src-tauri/src/commands/onboarding_commands.rs
autonomous: true
requirements:
  - CTX-01
  - CTX-02
  - CTX-04

must_haves:
  truths:
    - "Context file for a project with no tasks/phases contains planning instructions appropriate to the tier"
    - "Context file for a project with in-progress tasks contains execution guidance and progress summary"
    - "Context file for a project with 50+ tasks summarizes completed phases as one-liners rather than listing every task"
    - "Context file for a Quick-tier project contains simple todo-list prompts"
    - "Context file for a GSD-tier project contains GSD command instructions and no output contract"
    - "Context file stays within soft ~2000 token budget via progressive phase collapse"
  artifacts:
    - path: "src-tauri/src/models/onboarding.rs"
      provides: "ProjectState enum, detect_project_state(), get_instructions(), build_work_section(), estimate_tokens(), generate_context_file_content(data, tier)"
      contains: "pub enum ProjectState"
    - path: "src-tauri/src/commands/onboarding_commands.rs"
      provides: "Tier resolution from project.planning_tier with default to quick"
      contains: "unwrap_or(\"quick\")"
  key_links:
    - from: "src-tauri/src/commands/onboarding_commands.rs"
      to: "src-tauri/src/models/onboarding.rs"
      via: "generate_context_file_content(&context_data, tier)"
      pattern: "generate_context_file_content.*tier"
---

<objective>
Refactor the context file generator to produce adaptive, state-aware, tier-aware, token-budget-respecting context files.

Purpose: The AI context file (.element/context.md) currently has two modes (empty vs populated). This plan replaces it with a 4-state x 3-tier matrix that gives the AI tool the right instructions for the project's current situation, while keeping large projects concise via progressive phase rollup.

Output: Updated onboarding.rs with ProjectState enum, instruction matrix, token budget logic; updated onboarding_commands.rs to pass tier; all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-adaptive-context-builder/13-CONTEXT.md
@.planning/phases/13-adaptive-context-builder/13-RESEARCH.md
@.planning/phases/13-adaptive-context-builder/13-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src-tauri/src/models/onboarding.rs (current):
```rust
pub struct ProjectContextData {
    pub project_name: String,
    pub project_description: String,
    pub phases: Vec<PhaseContextData>,
    pub unassigned_tasks: Vec<TaskContextData>,
    pub total_tasks: usize,
    pub completed_tasks: usize,
    pub in_progress_tasks: usize,
    pub is_empty: bool,
}

pub struct PhaseContextData {
    pub name: String,
    pub sort_order: i32,
    pub tasks: Vec<TaskContextData>,
    pub completed: usize,
    pub total: usize,
}

pub struct TaskContextData {
    pub title: String,
    pub status: String,
    pub description: String,
}

// Current signature (will change):
pub fn generate_context_file_content(data: &ProjectContextData) -> String;
fn status_icon(status: &str) -> &'static str;
fn output_contract_section() -> String;
```

From src-tauri/src/commands/onboarding_commands.rs (lines 181-291):
```rust
// Current call site:
let content = crate::models::onboarding::generate_context_file_content(&context_data);
// Must become:
// let tier = project.planning_tier.as_deref().unwrap_or("quick");
// let content = crate::models::onboarding::generate_context_file_content(&context_data, tier);
```

From src-tauri/src/models/project.rs:
```rust
pub struct Project {
    pub id: String,
    pub name: String,
    pub description: String,
    pub directory_path: Option<String>,
    pub theme_id: Option<String>,
    pub created_at: String,
    pub updated_at: String,
    // Phase 12 will add: pub planning_tier: Option<String>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: State detection, instruction matrix, and tier-aware content generation with TDD</name>
  <files>src-tauri/src/models/onboarding.rs</files>
  <read_first>
    - src-tauri/src/models/onboarding.rs (current implementation -- 389 lines, all functions and tests)
    - .planning/phases/13-adaptive-context-builder/13-UI-SPEC.md (instruction matrix exact copy in "Instruction Matrix" section)
    - .planning/phases/13-adaptive-context-builder/13-CONTEXT.md (decisions D-01 through D-13)
    - .planning/phases/13-adaptive-context-builder/13-RESEARCH.md (code examples and patterns)
  </read_first>
  <behavior>
    State detection (CTX-01):
    - Test: detect_project_state with is_empty=true, no phases, no tasks -> NoPlan
    - Test: detect_project_state with phases but no tasks -> NoPlan (per D-01: "no tasks AND no phases")
    - Test: detect_project_state with tasks all pending -> Planned
    - Test: detect_project_state with at least one in-progress task -> InProgress
    - Test: detect_project_state with all tasks complete (total > 0) -> Complete
    - Test: detect_project_state with only blocked tasks (no in-progress, no complete) -> Planned

    Instruction matrix (CTX-04):
    - Test: get_instructions(NoPlan, "quick") returns "Describe what you need done. I'll create a simple task list."
    - Test: get_instructions(NoPlan, "medium") returns "Let's break this into phases. What are the major deliverables?"
    - Test: get_instructions(NoPlan, "full") returns "This project uses GSD workflow. Run /gsd:new-project to begin."
    - Test: get_instructions(Planned, "quick") returns "You have a task list below. Pick one and start working on it."
    - Test: get_instructions(Planned, "medium") returns "Review the phases below. Start with the first incomplete phase. Ask if you need clarification."
    - Test: get_instructions(Planned, "full") returns "This project uses GSD workflow. Run /gsd:progress to see status. Run /gsd:next to continue."
    - Test: get_instructions(InProgress, "quick") returns "You have a simple task list. Check off items as you complete them."
    - Test: get_instructions(InProgress, "medium") returns "Review the phases below. Focus on the current phase. Ask if you need clarification."
    - Test: get_instructions(InProgress, "full") returns "This project uses GSD workflow. Run /gsd:progress to see status. Run /gsd:next to continue."
    - Test: get_instructions(Complete, "quick") returns "All tasks are complete. Consider marking this project as done, or add follow-up work if needed."
    - Test: get_instructions(Complete, "full") returns same completion message (all tiers same for Complete per D-03)

    Token budget (CTX-02):
    - Test: classify_phase with all tasks complete -> Completed
    - Test: classify_phase with in-progress task -> Active
    - Test: classify_phase with all pending tasks -> Future
    - Test: classify_phases with no Active phase promotes first Future to Active
    - Test: estimate_tokens("hello world") returns approximately len/4
    - Test: build_work_section with 10+ completed phases produces one-line summaries ("Phase N: {name} [{done}/{total} complete]")
    - Test: build_work_section with 50+ tasks in completed phases does NOT list individual tasks
    - Test: progressive collapse -- when first-pass output exceeds ~2000 tokens, only active phase shows task listing

    Full content generation (all requirements):
    - Test: generate_context_file_content for NoPlan+quick contains "# {project_name}", "**Tier:** Quick", "**State:** No plan", instruction text, output contract section
    - Test: generate_context_file_content for NoPlan+full contains GSD instructions, does NOT contain "## Output Contract" (D-10)
    - Test: generate_context_file_content for InProgress+medium contains "## What Needs Attention", "## Current Work", phase rollup
    - Test: generate_context_file_content for Complete+any contains completion nudge message (D-03)
    - Test: output contract section NOT rendered when tier is "full" (D-10, D-13)
    - Test: output contract section rendered for "quick" tier in NoPlan state
    - Test: output contract NOT rendered in Planned/InProgress/Complete states even for quick/medium (per research recommendation)
    - Test: long description (>500 chars) is truncated at sentence boundary with "..."
  </behavior>
  <action>
    RED phase: Write all test functions first. Create a helper function `make_test_data(project_name, phases_config, tier_config)` that builds ProjectContextData with various configurations to reduce boilerplate. Run `cargo test -p element --lib models::onboarding` -- all new tests must FAIL (existing tests will also fail once signature changes, which is expected).

    GREEN phase: Implement in this order:

    1. Add `ProjectState` enum with four variants: `NoPlan`, `Planned`, `InProgress`, `Complete`. Derive Debug, Clone, PartialEq.

    2. Add `detect_project_state(data: &ProjectContextData) -> ProjectState`:
       - If `data.phases.is_empty() && data.total_tasks == 0` -> NoPlan (per D-01)
       - If `data.total_tasks > 0 && data.completed_tasks == data.total_tasks` -> Complete
       - If `data.in_progress_tasks > 0` -> InProgress
       - If `data.total_tasks > 0` -> Planned
       - Else -> NoPlan (edge case: phases exist but no tasks)

    3. Add `get_instructions(state: &ProjectState, tier: &str) -> &'static str` using match expression. Exact copy from 13-UI-SPEC.md instruction matrix:
       - (NoPlan, "quick") -> "Describe what you need done. I'll create a simple task list."
       - (NoPlan, "medium") -> "Let's break this into phases. What are the major deliverables?"
       - (NoPlan, "full") -> "This project uses GSD workflow. Run /gsd:new-project to begin."
       - (Planned, "quick") -> "You have a task list below. Pick one and start working on it."
       - (Planned, "medium") -> "Review the phases below. Start with the first incomplete phase. Ask if you need clarification."
       - (Planned, "full") -> "This project uses GSD workflow. Run /gsd:progress to see status. Run /gsd:next to continue."
       - (InProgress, "quick") -> "You have a simple task list. Check off items as you complete them."
       - (InProgress, "medium") -> "Review the phases below. Focus on the current phase. Ask if you need clarification."
       - (InProgress, "full") -> "This project uses GSD workflow. Run /gsd:progress to see status. Run /gsd:next to continue."
       - (Complete, _) -> "All tasks are complete. Consider marking this project as done, or add follow-up work if needed."
       - (_, _) fallback -> "Review the tasks below and continue working."

    4. Add `PhaseClass` enum: `Completed`, `Active`, `Future`. Add `classify_phase()` and `classify_phases()` per research code examples.

    5. Add `estimate_tokens(text: &str) -> usize` returning `text.len() / 4`.

    6. Add `const SOFT_TOKEN_BUDGET: usize = 2000;`

    7. Add `format_phase_rollup(phase: &PhaseContextData, class: &PhaseClass) -> String`:
       - Completed: `"- {name} [{completed}/{total} complete]\n"`
       - Active: `"### {name} [{completed}/{total}]\n\n"` + per-task `"- {status_icon} {title}\n"` (titles only per D-06, no descriptions)
       - Future: `"- {name} [{total} tasks]\n"`

    8. Add `build_header(data: &ProjectContextData, tier: &str, state: &ProjectState) -> String`:
       - `"# {project_name}\n\n"`
       - Description: if len > 500, truncate at last `. ` before 500 chars and append `"..."`. Else verbatim. Skip if empty.
       - `"\n\n**Tier:** {tier_display}\n"` where tier_display is "Quick"/"Medium"/"GSD" (capitalize "quick"->"Quick", "medium"->"Medium", "full"->"GSD")
       - `"**State:** {state_display}\n"` where state_display is "No plan"/"Planned"/"In progress"/"Complete"
       - `"**Progress:** {completed}/{total} tasks complete across {phase_count} phases\n\n"` (skip if NoPlan)

    9. Add `build_instructions(state: &ProjectState, tier: &str) -> String`:
       - `"## Instructions\n\n{get_instructions(state, tier)}\n\n"`

    10. Add `build_attention_section(data: &ProjectContextData) -> String`:
        - Preserve existing logic from `generate_populated_project_context`: collect in-progress tasks first, then pending, take up to 5.
        - `"## What Needs Attention\n\n"` + items as `"- **{title}** ({status})\n"`.
        - If no items: `"All tasks complete!\n"`.
        - Skip section entirely if NoPlan state (no tasks to show).

    11. Add `build_work_section(data: &ProjectContextData, state: &ProjectState) -> String`:
        - Uses classify_phases + format_phase_rollup per research code.
        - First pass: normal rollup. If estimate_tokens > SOFT_TOKEN_BUDGET, second pass: only active phase gets full listing, everything else gets one-line summary.
        - Also include unassigned tasks section if any exist.

    12. Refactor `generate_context_file_content` signature to `(data: &ProjectContextData, tier: &str) -> String`:
        - Pipeline: build_header -> build_instructions -> build_attention_section (if not NoPlan) -> build_work_section (if not NoPlan) -> output_contract_section (only if tier != "full" AND state == NoPlan per D-10/D-13 and research recommendation).
        - Remove the old `generate_empty_project_context` and `generate_populated_project_context` functions (replaced by the pipeline).
        - Keep `status_icon()`, `output_contract_section()`, and `parse_plan_output_file()` unchanged.

    13. Update the 3 existing tests to use the new signature `generate_context_file_content(&data, "quick")` and update assertions to match new header format (`"# My App"` instead of `"# Project Context: My App"`). The existing test for populated project should assert `"## Current Work"` instead of `"## Phases"`. The empty project test should assert `"## Instructions"` instead of `"## Your Task"`.

    REFACTOR phase: Extract any repeated string patterns into constants if needed. Ensure all functions are `pub(crate)` or `pub` as appropriate -- `ProjectState`, `detect_project_state`, `get_instructions`, and `generate_context_file_content` should be `pub`. Internal helpers (`build_header`, `build_instructions`, etc.) can be private `fn`.
  </action>
  <verify>
    <automated>cd /Users/cuhnowts/projects/element/src-tauri && cargo test -p element --lib models::onboarding 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - onboarding.rs contains `pub enum ProjectState` with variants `NoPlan`, `Planned`, `InProgress`, `Complete`
    - onboarding.rs contains `pub fn detect_project_state(data: &ProjectContextData) -> ProjectState`
    - onboarding.rs contains `pub fn get_instructions(state: &ProjectState, tier: &str) -> &'static str`
    - onboarding.rs contains `fn estimate_tokens(text: &str) -> usize`
    - onboarding.rs contains `const SOFT_TOKEN_BUDGET: usize = 2000`
    - onboarding.rs contains `pub fn generate_context_file_content(data: &ProjectContextData, tier: &str) -> String`
    - onboarding.rs does NOT contain `fn generate_empty_project_context` (removed)
    - onboarding.rs does NOT contain `fn generate_populated_project_context` (removed)
    - onboarding.rs contains at least 14 test functions in `mod tests`
    - `cargo test -p element --lib models::onboarding` exits 0 with all tests passing
    - Test `test_instructions_gsd` asserts the string "/gsd:new-project"
    - Test `test_output_contract_gsd_excluded` asserts output does NOT contain "## Output Contract"
    - Test `test_token_budget_large_project` uses at least 10 phases to verify rollup
  </acceptance_criteria>
  <done>
    ProjectState enum detects 4 states from task data. Instruction matrix returns correct text for all 12 state+tier combinations. Token budget rollup collapses completed phases to one-liners and progressively collapses when over ~2000 tokens. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire tier into command handler and verify full compilation</name>
  <files>src-tauri/src/commands/onboarding_commands.rs</files>
  <read_first>
    - src-tauri/src/commands/onboarding_commands.rs (full file -- need to see generate_context_file function at lines 181-291)
    - src-tauri/src/models/project.rs (Project struct -- check if planning_tier field exists yet from Phase 12)
    - src-tauri/src/models/onboarding.rs (verify new signature from Task 1)
  </read_first>
  <action>
    Update the `generate_context_file` Tauri command in onboarding_commands.rs (lines 181-291) to pass the planning tier to the refactored content generator.

    **If Phase 12 has already added `planning_tier` to Project struct:**
    After line 191 (where `project` is fetched), add:
    ```rust
    let tier = project.planning_tier.as_deref().unwrap_or("quick");
    ```
    Then change line 279 from:
    ```rust
    let content = crate::models::onboarding::generate_context_file_content(&context_data);
    ```
    to:
    ```rust
    let content = crate::models::onboarding::generate_context_file_content(&context_data, tier);
    ```

    **If Phase 12 has NOT yet added `planning_tier` to Project struct:**
    Hardcode the default tier until Phase 12 adds the field:
    ```rust
    // TODO: Phase 12 adds planning_tier to Project struct. Until then, default to "quick" per D-09.
    let tier = "quick";
    ```
    Then change the call to:
    ```rust
    let content = crate::models::onboarding::generate_context_file_content(&context_data, tier);
    ```

    No other changes needed in this file. The context data assembly logic (lines 204-277) remains unchanged.
  </action>
  <verify>
    <automated>cd /Users/cuhnowts/projects/element/src-tauri && cargo build -p element 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - onboarding_commands.rs contains `generate_context_file_content(&context_data, tier)` (with tier parameter)
    - onboarding_commands.rs contains either `project.planning_tier.as_deref().unwrap_or("quick")` OR `let tier = "quick"` with a TODO comment mentioning Phase 12
    - `cargo build -p element` exits 0 with no errors
    - `cargo test -p element` exits 0 with all tests passing (including the updated onboarding tests from Task 1)
  </acceptance_criteria>
  <done>
    Command handler passes tier to the adaptive context builder. Full project compiles and all tests pass. The context generation pipeline is end-to-end functional.
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p element --lib models::onboarding` -- all unit tests pass (state detection, instruction matrix, token budget, content generation)
2. `cargo build -p element` -- full project compiles with no errors
3. `cargo test -p element` -- full test suite passes (no regressions)
</verification>

<success_criteria>
- Context file adapts content based on project state: NO_PLAN shows planning instructions, PLANNED shows task list guidance, IN_PROGRESS shows execution guidance with progress, COMPLETE shows completion nudge (CTX-01)
- Large projects (50+ tasks across 10+ phases) produce context files with phase-level rollup summaries rather than individual task listings, staying within soft ~2000 token budget (CTX-02)
- Quick tier produces simple todo-list prompts, Medium produces phase-focused guidance, GSD produces /gsd command instructions. GSD tier excludes output contract section (CTX-04)
- All 14+ unit tests pass via `cargo test -p element --lib models::onboarding`
- Full project compiles and passes `cargo test -p element`
</success_criteria>

<output>
After completion, create `.planning/phases/13-adaptive-context-builder/13-01-SUMMARY.md`
</output>
