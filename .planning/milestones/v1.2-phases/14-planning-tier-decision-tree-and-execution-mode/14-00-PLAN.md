---
phase: 14-planning-tier-decision-tree-and-execution-mode
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/center/TierSelectionDialog.test.tsx
  - src/components/center/OpenAiButton.test.tsx
  - src/components/center/__tests__/ProjectDetail.test.tsx
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, CTX-03]
must_haves:
  truths:
    - "Test stubs exist for TierSelectionDialog before implementation begins"
    - "OpenAiButton test file is updated with tier gate stubs"
    - "ProjectDetail test file has tier badge and change plan stubs"
  artifacts:
    - path: "src/components/center/TierSelectionDialog.test.tsx"
      provides: "Failing test stubs for tier dialog component"
    - path: "src/components/center/OpenAiButton.test.tsx"
      provides: "Updated stubs for tier gate logic"
    - path: "src/components/center/__tests__/ProjectDetail.test.tsx"
      provides: "Updated stubs for tier badge and change plan button"
  key_links: []
---

<objective>
Create failing test stubs for Phase 14 components before any implementation begins.

Purpose: Nyquist compliance requires test files to exist before Wave 1 execution so that implementation tasks can run vitest as their verify command and get meaningful red-to-green feedback.

Output: Three test files with `it.todo()` or `it.skip()` stubs covering all Phase 14 requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-planning-tier-decision-tree-and-execution-mode/14-CONTEXT.md
@.planning/phases/14-planning-tier-decision-tree-and-execution-mode/14-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TierSelectionDialog test stubs</name>
  <files>src/components/center/TierSelectionDialog.test.tsx</files>
  <read_first>
    - src/components/center/OpenAiButton.test.tsx (reference for test patterns, mocking approach)
    - src/components/center/__tests__/ProjectDetail.test.tsx (reference for test patterns)
  </read_first>
  <action>
Create `src/components/center/TierSelectionDialog.test.tsx` with failing stubs using `it.todo()` for all Phase 14 tier dialog behaviors:

```typescript
import { describe, it } from "vitest";

describe("TierSelectionDialog", () => {
  // PLAN-01: Tier dialog renders with three options
  it.todo("renders three tier options: Quick, Medium, GSD");
  it.todo("shows 'How should we plan this?' as dialog title");
  it.todo("shows description textarea with 'What are you building?' label");

  // PLAN-01: Validation
  it.todo("disables Start Planning when no tier is selected");
  it.todo("shows validation error when Quick tier selected without description");

  // PLAN-02: Quick tier
  it.todo("calls onSubmit with 'quick' tier and description");

  // PLAN-03: Medium tier
  it.todo("calls onSubmit with 'medium' tier and description");

  // PLAN-04: GSD tier
  it.todo("calls onSubmit with 'full' tier and description");
  it.todo("displays 'GSD' label for full tier value");

  // D-04: Change plan flow
  it.todo("shows warning dialog when isChangingTier is true");
  it.todo("proceeds to tier selection after warning confirmation");
  it.todo("closes entirely on warning dismissal");
});
```

This file should compile and run (all tests show as todo/pending, none fail). The stubs will be fleshed out by Wave 1 executors alongside implementation.
  </action>
  <verify>
    <automated>cd /Users/cuhnowts/projects/element && npx vitest run src/components/center/TierSelectionDialog.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>TierSelectionDialog.test.tsx exists with todo stubs for all PLAN-01/02/03/04 behaviors. vitest runs the file without errors (all tests show as todo).</done>
</task>

<task type="auto">
  <name>Task 2: Update OpenAiButton and ProjectDetail test stubs for tier gate</name>
  <files>src/components/center/OpenAiButton.test.tsx, src/components/center/__tests__/ProjectDetail.test.tsx</files>
  <read_first>
    - src/components/center/OpenAiButton.test.tsx (current tests to extend)
    - src/components/center/__tests__/ProjectDetail.test.tsx (current tests to extend)
  </read_first>
  <action>
**1. Update `src/components/center/OpenAiButton.test.tsx`:**

Add new `describe` block with todo stubs for Phase 14 tier gate behavior:

```typescript
describe("Phase 14: Tier gate", () => {
  // PLAN-01: Dialog gate
  it.todo("opens tier dialog when project has no tier and no tasks");
  it.todo("skips tier dialog when project has a stored tier");
  it.todo("skips tier dialog when project has existing tasks");

  // PLAN-04: GSD behavior
  it.todo("skips plan watcher when tier is 'full' (GSD)");
  it.todo("starts plan watcher when tier is 'quick'");
  it.todo("starts plan watcher when tier is 'medium'");

  // D-07: Context file regeneration after confirm
  it.todo("regenerates context file after plan confirmation");
});
```

Append this block to the existing test file. Do NOT modify existing tests.

**2. Update `src/components/center/__tests__/ProjectDetail.test.tsx`:**

Add new `describe` block with todo stubs for Phase 14 header changes:

```typescript
describe("Phase 14: Tier badge and change plan", () => {
  // PLAN-01: Tier badge
  it.todo("shows tier badge when project has planningTier set");
  it.todo("shows 'GSD' text for full tier");
  it.todo("shows 'Quick' text for quick tier");
  it.todo("shows 'Medium' text for medium tier");

  // D-04: Change plan
  it.todo("shows 'Change plan' button when tier is set");
  it.todo("opens tier dialog with isChangingTier when Change plan clicked");
});
```

Append this block to the existing test file. Do NOT modify existing tests.
  </action>
  <verify>
    <automated>cd /Users/cuhnowts/projects/element && npx vitest run src/components/center/OpenAiButton.test.tsx src/components/center/__tests__/ProjectDetail.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Both test files updated with Phase 14 todo stubs. Existing tests still pass. New stubs show as todo in vitest output.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` passes (existing tests green, new stubs show as todo)
- TierSelectionDialog.test.tsx exists
- OpenAiButton.test.tsx has "Phase 14: Tier gate" describe block
- ProjectDetail.test.tsx has "Phase 14: Tier badge and change plan" describe block
</verification>

<success_criteria>
- All three test files run without errors via vitest
- Todo stubs cover all PLAN-01, PLAN-02, PLAN-03, PLAN-04, CTX-03 requirements
- Existing tests are unaffected
- Wave 1 plans can use `npx vitest run <file> -x` as their verify command
</success_criteria>

<output>
After completion, create `.planning/phases/14-planning-tier-decision-tree-and-execution-mode/14-00-SUMMARY.md`
</output>
