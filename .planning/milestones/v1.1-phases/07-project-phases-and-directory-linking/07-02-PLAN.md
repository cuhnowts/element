---
phase: 07-project-phases-and-directory-linking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/types.ts
  - src/lib/tauri.ts
  - src/stores/phaseSlice.ts
  - src/stores/projectSlice.ts
  - src/stores/taskSlice.ts
  - src/stores/index.ts
  - src/components/ui/context-menu.tsx
  - src/components/ui/collapsible.tsx
  - src/components/ui/progress.tsx
  - src/components/ui/select.tsx
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04
must_haves:
  truths:
    - "Phase TypeScript type exists with id, projectId, name, sortOrder, createdAt, updatedAt"
    - "Project type includes directoryPath: string | null"
    - "Task type includes phaseId: string | null"
    - "API layer has methods for all 7 phase/directory/task-phase commands"
    - "Zustand store has phase slice with loadPhases, createPhase, updatePhase, deletePhase, reorderPhases"
    - "Project slice has linkDirectory action"
    - "Task slice has setTaskPhase action that accepts nullable phaseId"
    - "Task slice createTask accepts optional phaseId"
    - "All 4 shadcn components installed: context-menu, collapsible, progress, select"
    - "dnd-kit packages installed: @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities"
    - "@tauri-apps/plugin-dialog installed"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "Phase interface and extended Project/Task types"
      contains: "export interface Phase"
    - path: "src/lib/tauri.ts"
      provides: "API methods for phase CRUD, directory linking, and task phase assignment"
      contains: "createPhase"
    - path: "src/stores/phaseSlice.ts"
      provides: "Zustand phase state and actions"
      contains: "createPhaseSlice"
    - path: "src/stores/projectSlice.ts"
      provides: "Extended project slice with linkDirectory"
      contains: "linkDirectory"
  key_links:
    - from: "src/stores/phaseSlice.ts"
      to: "src/lib/tauri.ts"
      via: "api.createPhase, api.listPhases, etc."
      pattern: "api\\.createPhase"
    - from: "src/stores/index.ts"
      to: "src/stores/phaseSlice.ts"
      via: "createPhaseSlice spread into store"
      pattern: "createPhaseSlice"
    - from: "src/lib/tauri.ts"
      to: "Tauri invoke"
      via: "invoke<Phase> calls"
      pattern: 'invoke.*"create_phase"'
    - from: "src/stores/taskSlice.ts"
      to: "src/lib/tauri.ts"
      via: "api.setTaskPhase call"
      pattern: "api\\.setTaskPhase"
---

<objective>
Install all npm dependencies, shadcn components, and build the frontend type/API/store layer for phases and directory linking.

Purpose: Give the UI layer (Plan 03) a complete data substrate -- types, API calls, and Zustand state management -- so it can focus purely on rendering and interaction. Includes dedicated `setTaskPhase` API/store action for explicit phase assignment/unassignment.

Output: All npm packages installed, types extended, API layer complete, Zustand store wired with phase slice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-project-phases-and-directory-linking/07-CONTEXT.md
@.planning/phases/07-project-phases-and-directory-linking/07-RESEARCH.md

<interfaces>
<!-- Current types the executor must extend (not replace) -->

From src/lib/types.ts:
```typescript
export interface Project {
  id: string;
  name: string;
  description: string;
  createdAt: string;
  updatedAt: string;
}

export interface Task {
  id: string;
  projectId: string;
  title: string;
  description: string;
  context: string;
  status: TaskStatus;
  priority: TaskPriority;
  externalPath: string | null;
  dueDate: string | null;
  scheduledDate: string | null;
  scheduledTime: string | null;
  durationMinutes: number | null;
  recurrenceRule: string | null;
  createdAt: string;
  updatedAt: string;
}

export interface CreateTaskInput {
  projectId: string;
  title: string;
  description?: string;
  context?: string;
  priority?: TaskPriority;
  externalPath?: string;
  dueDate?: string;
  scheduledDate?: string;
  scheduledTime?: string;
  durationMinutes?: number;
  recurrenceRule?: string;
}
```

From src/lib/tauri.ts:
```typescript
import { invoke } from "@tauri-apps/api/core";
// api object with methods for projects, tasks, etc.
```

From src/stores/index.ts:
```typescript
export type AppStore = ProjectSlice & TaskSlice & UiSlice & PluginSlice & CredentialSlice & CalendarSlice & SchedulingSlice & AiSlice;
export const useStore = create<AppStore>()((...a) => ({
  ...createProjectSlice(...a),
  ...createTaskSlice(...a),
  // etc.
}));
```

From src/stores/projectSlice.ts:
```typescript
export interface ProjectSlice {
  projects: Project[];
  selectedProjectId: string | null;
  projectsLoading: boolean;
  loadProjects: () => Promise<void>;
  createProject: (name: string, description?: string) => Promise<Project>;
  deleteProject: (projectId: string) => Promise<void>;
  selectProject: (projectId: string | null) => void;
}
```

From src/stores/taskSlice.ts:
```typescript
export interface TaskSlice {
  tasks: Task[];
  selectedTaskId: string | null;
  selectedTask: TaskWithTags | null;
  tasksLoading: boolean;
  loadTasks: (projectId: string) => Promise<void>;
  createTask: (projectId: string, title: string) => Promise<Task>;
  // ... other methods
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install npm packages and shadcn components</name>
  <files>package.json</files>
  <read_first>
    package.json
  </read_first>
  <action>
    Run these install commands from the project root (`/Users/cuhnowts/projects/element`):

    1. Install npm packages:
       ```bash
       npm install @tauri-apps/plugin-dialog @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
       ```

    2. Install shadcn components (run each separately since shadcn CLI is interactive):
       ```bash
       npx shadcn@latest add context-menu --yes
       npx shadcn@latest add collapsible --yes
       npx shadcn@latest add progress --yes
       npx shadcn@latest add select --yes
       ```

    Verify all packages appear in package.json dependencies after install.
  </action>
  <verify>
    <automated>cd /Users/cuhnowts/projects/element && node -e "const p=require('./package.json'); const d=p.dependencies; console.log(d['@tauri-apps/plugin-dialog'] ? 'OK dialog' : 'MISSING dialog'); console.log(d['@dnd-kit/core'] ? 'OK dnd-core' : 'MISSING dnd-core'); console.log(d['@dnd-kit/sortable'] ? 'OK sortable' : 'MISSING sortable'); console.log(d['@dnd-kit/utilities'] ? 'OK utilities' : 'MISSING utilities');"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "@tauri-apps/plugin-dialog" in dependencies
    - package.json contains "@dnd-kit/core" in dependencies
    - package.json contains "@dnd-kit/sortable" in dependencies
    - package.json contains "@dnd-kit/utilities" in dependencies
    - src/components/ui/context-menu.tsx exists
    - src/components/ui/collapsible.tsx exists
    - src/components/ui/progress.tsx exists
    - src/components/ui/select.tsx exists
  </acceptance_criteria>
  <done>All npm dependencies installed, all 4 shadcn components generated.</done>
</task>

<task type="auto">
  <name>Task 2: Extend types, API layer, and Zustand store with phase/directory/task-phase support</name>
  <files>
    src/lib/types.ts,
    src/lib/tauri.ts,
    src/stores/phaseSlice.ts,
    src/stores/projectSlice.ts,
    src/stores/taskSlice.ts,
    src/stores/index.ts
  </files>
  <read_first>
    src/lib/types.ts,
    src/lib/tauri.ts,
    src/stores/projectSlice.ts,
    src/stores/taskSlice.ts,
    src/stores/index.ts
  </read_first>
  <action>
    1. **Update `src/lib/types.ts`** -- Add these types/fields (do NOT remove any existing types):

       Add `Phase` interface after the `Tag` interface:
       ```typescript
       export interface Phase {
         id: string;
         projectId: string;
         name: string;
         sortOrder: number;
         createdAt: string;
         updatedAt: string;
       }
       ```

       Add `directoryPath: string | null;` to `Project` interface (after `description`):
       ```typescript
       export interface Project {
         id: string;
         name: string;
         description: string;
         directoryPath: string | null;  // NEW
         createdAt: string;
         updatedAt: string;
       }
       ```

       Add `phaseId: string | null;` to `Task` interface (after `recurrenceRule`, before `createdAt`):
       ```typescript
       // In Task interface, add:
       phaseId: string | null;
       ```

       Add `phaseId?: string;` to `CreateTaskInput` interface:
       ```typescript
       // In CreateTaskInput, add:
       phaseId?: string;
       ```

       Add `phaseId?: string;` to `UpdateTaskInput` interface:
       ```typescript
       // In UpdateTaskInput, add:
       phaseId?: string;
       ```

    2. **Update `src/lib/tauri.ts`** -- Add Phase and Task imports and API methods:

       Add `Phase` to the type import from `"./types"`.

       Add these methods to the `api` object after the Projects section:
       ```typescript
       // Phases
       createPhase: (projectId: string, name: string) =>
         invoke<Phase>("create_phase", { projectId, name }),
       listPhases: (projectId: string) =>
         invoke<Phase[]>("list_phases", { projectId }),
       updatePhase: (phaseId: string, name: string) =>
         invoke<Phase>("update_phase", { phaseId, name }),
       deletePhase: (phaseId: string) =>
         invoke<void>("delete_phase", { phaseId }),
       reorderPhases: (projectId: string, orderedIds: string[]) =>
         invoke<void>("reorder_phases", { projectId, orderedIds }),
       linkProjectDirectory: (projectId: string, directoryPath: string) =>
         invoke<Project>("link_project_directory", { projectId, directoryPath }),

       // Task phase assignment (dedicated command -- supports explicit null for unassign)
       setTaskPhase: (taskId: string, phaseId: string | null) =>
         invoke<Task>("set_task_phase", { taskId, phaseId }),
       ```

    3. **Create `src/stores/phaseSlice.ts`** following the exact pattern of projectSlice.ts:
       ```typescript
       import type { StateCreator } from "zustand";
       import { api } from "../lib/tauri";
       import type { Phase } from "../lib/types";
       import type { AppStore } from "./index";

       export interface PhaseSlice {
         phases: Phase[];
         phasesLoading: boolean;
         loadPhases: (projectId: string) => Promise<void>;
         createPhase: (projectId: string, name: string) => Promise<Phase>;
         updatePhase: (phaseId: string, name: string) => Promise<void>;
         deletePhase: (phaseId: string) => Promise<void>;
         reorderPhases: (projectId: string, orderedIds: string[]) => Promise<void>;
       }

       export const createPhaseSlice: StateCreator<
         AppStore,
         [],
         [],
         PhaseSlice
       > = (set, _get) => ({
         phases: [],
         phasesLoading: false,
         loadPhases: async (projectId) => {
           set({ phasesLoading: true });
           const phases = await api.listPhases(projectId);
           set({ phases, phasesLoading: false });
         },
         createPhase: async (projectId, name) => {
           const phase = await api.createPhase(projectId, name);
           set((s) => ({ phases: [...s.phases, phase] }));
           return phase;
         },
         updatePhase: async (phaseId, name) => {
           const phase = await api.updatePhase(phaseId, name);
           set((s) => ({
             phases: s.phases.map((p) => (p.id === phaseId ? phase : p)),
           }));
         },
         deletePhase: async (phaseId) => {
           await api.deletePhase(phaseId);
           set((s) => ({
             phases: s.phases.filter((p) => p.id !== phaseId),
           }));
         },
         reorderPhases: async (projectId, orderedIds) => {
           // Optimistic update
           set((s) => ({
             phases: orderedIds
               .map((id, index) => {
                 const phase = s.phases.find((p) => p.id === id);
                 return phase ? { ...phase, sortOrder: index } : null;
               })
               .filter((p): p is Phase => p !== null),
           }));
           try {
             await api.reorderPhases(projectId, orderedIds);
           } catch {
             // Revert on failure
             const phases = await api.listPhases(projectId);
             set({ phases });
           }
         },
       });
       ```

    4. **Update `src/stores/projectSlice.ts`** -- Add `linkDirectory` action:

       Add to `ProjectSlice` interface:
       ```typescript
       linkDirectory: (projectId: string, directoryPath: string) => Promise<void>;
       ```

       Add implementation in the createProjectSlice return object:
       ```typescript
       linkDirectory: async (projectId, directoryPath) => {
         const project = await api.linkProjectDirectory(projectId, directoryPath);
         set((s) => ({
           projects: s.projects.map((p) => (p.id === projectId ? project : p)),
         }));
       },
       ```

    5. **Update `src/stores/taskSlice.ts`** -- Extend createTask to accept phaseId and add setTaskPhase:

       Change the `createTask` signature in `TaskSlice` interface:
       ```typescript
       createTask: (projectId: string, title: string, phaseId?: string) => Promise<Task>;
       ```

       Add `setTaskPhase` to `TaskSlice` interface:
       ```typescript
       setTaskPhase: (taskId: string, phaseId: string | null) => Promise<void>;
       ```

       Update createTask implementation:
       ```typescript
       createTask: async (projectId, title, phaseId) => {
         const task = await api.createTask({ projectId, title, phaseId });
         set((s) => ({ tasks: [task, ...s.tasks] }));
         return task;
       },
       ```

       Add setTaskPhase implementation:
       ```typescript
       setTaskPhase: async (taskId, phaseId) => {
         const task = await api.setTaskPhase(taskId, phaseId);
         set((s) => ({
           tasks: s.tasks.map((t) => (t.id === taskId ? { ...t, phaseId: task.phaseId } : t)),
           // Also update selectedTask if it matches
           selectedTask: s.selectedTask?.id === taskId
             ? { ...s.selectedTask, phaseId: task.phaseId }
             : s.selectedTask,
         }));
       },
       ```

    6. **Update `src/stores/index.ts`** -- Wire in PhaseSlice:

       Add import:
       ```typescript
       import { createPhaseSlice, type PhaseSlice } from "./phaseSlice";
       ```

       Add `PhaseSlice` to the `AppStore` type union:
       ```typescript
       export type AppStore = ProjectSlice & TaskSlice & UiSlice & PluginSlice & CredentialSlice & CalendarSlice & SchedulingSlice & AiSlice & PhaseSlice;
       ```

       Add `...createPhaseSlice(...a),` to the useStore create call.
  </action>
  <verify>
    <automated>cd /Users/cuhnowts/projects/element && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/types.ts contains "export interface Phase" with "projectId: string" and "sortOrder: number"
    - src/lib/types.ts Project interface contains "directoryPath: string | null"
    - src/lib/types.ts Task interface contains "phaseId: string | null"
    - src/lib/types.ts CreateTaskInput contains "phaseId?: string"
    - src/lib/tauri.ts contains 'invoke<Phase>("create_phase"' and 'invoke<Phase[]>("list_phases"' and 'invoke<void>("reorder_phases"' and 'invoke<Project>("link_project_directory"'
    - src/lib/tauri.ts contains 'invoke<Task>("set_task_phase"' with "taskId, phaseId"
    - src/stores/phaseSlice.ts contains "export interface PhaseSlice" and "export const createPhaseSlice"
    - src/stores/phaseSlice.ts contains "loadPhases", "createPhase", "updatePhase", "deletePhase", "reorderPhases"
    - src/stores/projectSlice.ts contains "linkDirectory"
    - src/stores/taskSlice.ts contains "setTaskPhase" in interface and implementation
    - src/stores/taskSlice.ts createTask function body contains "phaseId"
    - src/stores/index.ts contains "PhaseSlice" and "createPhaseSlice"
    - `npx tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>Frontend types, API layer, and Zustand store fully extended with phase, directory, and task-phase support. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
TypeScript compiles with no errors: `npx tsc --noEmit`
All existing tests still pass: `npm test -- --run`
</verification>

<success_criteria>
- Phase interface exists in types.ts with all 6 fields
- Project has directoryPath, Task has phaseId in types
- API layer has all 7 new invoke methods (including setTaskPhase)
- PhaseSlice wired into Zustand store with CRUD + reorder + optimistic updates
- ProjectSlice has linkDirectory action
- TaskSlice has setTaskPhase action and createTask accepts optional phaseId
- All shadcn components installed (context-menu, collapsible, progress, select)
- All npm dependencies installed (plugin-dialog, dnd-kit)
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/07-project-phases-and-directory-linking/07-02-SUMMARY.md`
</output>
