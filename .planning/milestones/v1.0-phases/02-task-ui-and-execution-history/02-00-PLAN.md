---
phase: 02-task-ui-and-execution-history
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/setup.ts
  - src/components/sidebar/CalendarToggle.test.tsx
  - src/components/sidebar/TaskList.test.tsx
  - src/components/center/TaskDetail.test.tsx
  - src/components/center/ExecutionDiagram.test.tsx
  - src/components/output/LogViewer.test.tsx
  - src/stores/useTaskStore.test.ts
  - src/hooks/useTauriEvent.test.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - TASK-04

must_haves:
  truths:
    - "npx vitest run executes successfully with 0 failures"
    - "Tauri invoke and listen are mocked so tests run without a Tauri runtime"
    - "Each requirement (UI-01 through UI-05, TASK-04) has a corresponding test stub"
  artifacts:
    - path: "src/__tests__/setup.ts"
      provides: "Tauri invoke/listen mocks and jsdom test setup"
      contains: "vi.mock"
    - path: "src/components/sidebar/CalendarToggle.test.tsx"
      provides: "Test stub for UI-01 calendar toggle"
      contains: "describe"
    - path: "src/components/sidebar/TaskList.test.tsx"
      provides: "Test stub for UI-02 task list"
      contains: "describe"
    - path: "src/components/center/TaskDetail.test.tsx"
      provides: "Test stub for UI-03 task detail"
      contains: "describe"
    - path: "src/components/center/ExecutionDiagram.test.tsx"
      provides: "Test stub for UI-04 execution diagram"
      contains: "describe"
    - path: "src/components/output/LogViewer.test.tsx"
      provides: "Test stub for UI-05 log viewer"
      contains: "describe"
    - path: "src/stores/useTaskStore.test.ts"
      provides: "Test stub for TASK-04 execution history"
      contains: "describe"
    - path: "src/hooks/useTauriEvent.test.ts"
      provides: "Test stub for shared Tauri event hook"
      contains: "describe"
  key_links:
    - from: "src/__tests__/setup.ts"
      to: "vitest.config.ts"
      via: "setupFiles reference"
      pattern: "setupFiles.*setup"
---

<objective>
Create test infrastructure and stub test files for all Phase 2 requirements so that subsequent plans can run `npx vitest run` as their automated verification.

Purpose: Satisfies the Nyquist sampling contract -- every task commit in Plans 01-03 can run `npx vitest run` to get automated feedback. Without this Wave 0 plan, no automated test verification is possible.

Output: Vitest configured with Tauri mocks, 7 test stub files covering all 6 requirements, all passing (stubs use `it.todo` or trivial assertions).
</objective>

<execution_context>
@/Users/cuhnowts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cuhnowts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-task-ui-and-execution-history/02-CONTEXT.md
@.planning/phases/02-task-ui-and-execution-history/02-RESEARCH.md
@.planning/phases/02-task-ui-and-execution-history/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Vitest configuration and create test setup with Tauri mocks</name>
  <files>
    src/__tests__/setup.ts
  </files>
  <action>
Phase 1 is expected to have installed Vitest and created vitest.config.ts. Verify this exists. If not, install vitest and related dependencies:

```bash
npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom
```

Ensure vitest.config.ts (or vite.config.ts with test block) includes:
- `environment: 'jsdom'`
- `setupFiles: ['src/__tests__/setup.ts']`
- `globals: true`
- Path alias `@/` resolving to `src/`

Create `src/__tests__/setup.ts` with Tauri mocks:

```typescript
import { vi } from 'vitest';
import '@testing-library/jest-dom';

// Mock @tauri-apps/api/core (invoke)
vi.mock('@tauri-apps/api/core', () => ({
  invoke: vi.fn().mockResolvedValue(undefined),
}));

// Mock @tauri-apps/api/event (listen)
vi.mock('@tauri-apps/api/event', () => ({
  listen: vi.fn().mockResolvedValue(vi.fn()), // returns unlisten function
}));
```

This ensures all components that call `invoke()` or `listen()` will not crash in test environment.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>Vitest runs successfully with jsdom environment and Tauri mocks active. `npx vitest run` exits with code 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for all Phase 2 requirements</name>
  <files>
    src/components/sidebar/CalendarToggle.test.tsx,
    src/components/sidebar/TaskList.test.tsx,
    src/components/center/TaskDetail.test.tsx,
    src/components/center/ExecutionDiagram.test.tsx,
    src/components/output/LogViewer.test.tsx,
    src/stores/useTaskStore.test.ts,
    src/hooks/useTauriEvent.test.ts
  </files>
  <action>
Create 7 test stub files. Each file uses `describe` with `it.todo` placeholders for the behaviors that will be implemented in Plans 01-03. The stubs ensure `npx vitest run` passes (todo tests are skipped, not failed) and provide a skeleton that executors fill in as they build each component.

**src/components/sidebar/CalendarToggle.test.tsx** (UI-01):
```tsx
import { describe, it } from 'vitest';

describe('CalendarToggle', () => {
  it.todo('renders a Switch component');
  it.todo('reads calendarVisible from useWorkspaceStore');
  it.todo('calls toggleCalendar when switch is toggled');
  it.todo('renders "Calendar" label with correct typography classes');
});
```

**src/components/sidebar/TaskList.test.tsx** (UI-02):
```tsx
import { describe, it } from 'vitest';

describe('TaskList', () => {
  it.todo('calls fetchTodaysTasks on mount');
  it.todo('renders skeleton loading state when isLoadingTasks is true');
  it.todo('renders EmptyState when todaysTasks is empty');
  it.todo('renders TaskListItem for each task in todaysTasks');
  it.todo('renders "Today\'s Tasks" section header');
});
```

**src/components/center/TaskDetail.test.tsx** (UI-03):
```tsx
import { describe, it } from 'vitest';

describe('TaskDetail', () => {
  it.todo('fetches task detail when selectedTaskId changes');
  it.todo('fetches execution history when selectedTaskId changes');
  it.todo('renders TaskHeader with task data');
  it.todo('renders TaskMetadata with task data');
  it.todo('renders ExecutionDiagram with task steps');
  it.todo('shows skeleton loading state during fetch');
  it.todo('shows error message on fetch failure');
});
```

**src/components/center/ExecutionDiagram.test.tsx** (UI-04):
```tsx
import { describe, it } from 'vitest';

describe('ExecutionDiagram', () => {
  it.todo('renders StepItem for each step');
  it.todo('shows numbered circles for each step');
  it.todo('shows vertical connector lines between steps');
  it.todo('does not show connector after last step');
  it.todo('renders agent/skill/tool badges on steps');
  it.todo('shows EmptyState when steps array is empty');
});
```

**src/components/output/LogViewer.test.tsx** (UI-05):
```tsx
import { describe, it } from 'vitest';

describe('LogViewer', () => {
  it.todo('renders log entries with timestamp, level, and message');
  it.todo('applies correct color class for each log level');
  it.todo('shows EmptyState when entries array is empty');
  it.todo('auto-scrolls to bottom when new entries arrive and user is at bottom');
  it.todo('does not auto-scroll when user has scrolled up');
  it.todo('shows Jump to latest button when user scrolls up');
});
```

**src/stores/useTaskStore.test.ts** (TASK-04):
```typescript
import { describe, it } from 'vitest';

describe('useTaskStore', () => {
  it.todo('fetchTodaysTasks calls getTodaysTasks and updates state');
  it.todo('fetchTaskDetail calls getTaskDetail and updates selectedTaskDetail');
  it.todo('fetchExecutionHistory calls getExecutionHistory and updates state');
  it.todo('fetchExecutionLogs calls getExecutionLogs and updates executionLogs');
  it.todo('sets error state on fetch failure');
  it.todo('addLogEntry appends to executionLogs');
  it.todo('clearLogs resets executionLogs to empty');
});
```

**src/hooks/useTauriEvent.test.ts** (shared utility):
```typescript
import { describe, it } from 'vitest';

describe('useTauriEvent', () => {
  it.todo('subscribes to the specified event name');
  it.todo('calls handler with event payload');
  it.todo('calls unlisten on unmount');
});
```
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | grep -E "(Tests|Test Files)" | head -5</automated>
  </verify>
  <done>All 7 test files exist. `npx vitest run` exits with code 0. All tests are either passing or skipped (todo). No test failures.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` exits with code 0
2. All 7 test files are discovered by Vitest
3. Tauri invoke and listen mocks are active (no "Cannot find module" errors)
4. Todo tests are reported as skipped, not failed
</verification>

<success_criteria>
- Vitest runs in jsdom environment with Tauri mocks
- All 7 test stub files exist and are discovered
- `npx vitest run` exits cleanly with 0 failures
- Plans 01-03 can use `npx vitest run` as their automated verification command
</success_criteria>

<output>
After completion, create `.planning/phases/02-task-ui-and-execution-history/02-00-SUMMARY.md`
</output>
