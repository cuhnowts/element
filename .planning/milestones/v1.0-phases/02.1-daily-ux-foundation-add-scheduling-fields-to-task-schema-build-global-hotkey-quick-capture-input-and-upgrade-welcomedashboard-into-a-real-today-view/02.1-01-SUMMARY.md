---
phase: 02.1-daily-ux-foundation
plan: 01
subsystem: database, api, ui
tags: [sqlite, rusqlite, tauri, scheduling, migration, typescript]

requires:
  - phase: 01-core-engine
    provides: Task model with CRUD operations and SQLite schema
  - phase: 02-task-ui-execution
    provides: Execution commands, today view, task detail UI
provides:
  - 5 scheduling columns (due_date, scheduled_date, scheduled_time, duration_minutes, recurrence_rule) on tasks table
  - TASK_COLUMNS constant for consistent SELECT queries
  - Scheduling-aware get_todays_tasks query
  - Recurrence rule validation in Rust
  - Consolidated TypeScript Task type (single source of truth)
  - Scheduling fields in CreateTaskInput/UpdateTaskInput/updateTask store
affects: [02.1-02, 02.1-03, 02.1-04, quick-capture, today-view]

tech-stack:
  added: []
  patterns:
    - "TASK_COLUMNS constant to avoid column index fragility across queries"
    - "Re-export pattern: types/task.ts re-exports from lib/types.ts to eliminate dual type definitions"
    - "Recurrence rule validation in Rust model layer (not SQL CHECK constraint)"

key-files:
  created:
    - src-tauri/src/db/sql/003_scheduling.sql
  modified:
    - src-tauri/src/db/migrations.rs
    - src-tauri/src/models/task.rs
    - src-tauri/src/models/execution.rs
    - src-tauri/src/commands/task_commands.rs
    - src/lib/types.ts
    - src/types/task.ts
    - src/stores/taskSlice.ts

key-decisions:
  - "TASK_COLUMNS constant centralizes column list to prevent index drift across queries"
  - "Consolidated dual Task type system: types/task.ts now re-exports from lib/types.ts"
  - "Recurrence rule validation done in Rust (not SQL CHECK) to avoid ALTER TABLE issues"
  - "Option<String> semantics: None means dont-change, matching existing codebase convention"

patterns-established:
  - "TASK_COLUMNS: use format!(SELECT {} FROM tasks, TASK_COLUMNS) for all task queries"
  - "Type re-export: domain types defined once in lib/types.ts, re-exported by types/*.ts"

requirements-completed: [DUX-01, DUX-02, DUX-03]

duration: 6min
completed: 2026-03-16
---

# Phase 02.1 Plan 01: Task Scheduling Schema Summary

**SQLite migration adding 5 scheduling columns with indexes, Rust CRUD integration, scheduling-aware today query, and consolidated TypeScript types**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-17T01:57:05Z
- **Completed:** 2026-03-17T02:03:18Z
- **Tasks:** 2
- **Files modified:** 11

## Accomplishments
- Migration 003 adds due_date, scheduled_date, scheduled_time, duration_minutes, recurrence_rule columns with 2 indexes
- Full-stack scheduling field support: Rust structs, Tauri commands, TypeScript types, Zustand store
- Scheduling-aware get_todays_tasks: returns overdue tasks first, then today's scheduled, then unscheduled incomplete
- Eliminated dual Task type definitions by making types/task.ts re-export from lib/types.ts
- All 33 Rust tests pass including 4 new scheduling tests; TypeScript compiles clean

## Task Commits

Each task was committed atomically:

1. **Task 1: SQLite migration + Rust model/command changes** - `597853e` (feat)
2. **Task 2: Frontend type sync and API layer updates** - `4c85b1b` (feat)

## Files Created/Modified
- `src-tauri/src/db/sql/003_scheduling.sql` - Migration adding 5 scheduling columns + 2 indexes
- `src-tauri/src/db/migrations.rs` - Added version < 3 migration step
- `src-tauri/src/models/task.rs` - Task/CreateTaskInput/UpdateTaskInput with scheduling fields, TASK_COLUMNS, updated CRUD
- `src-tauri/src/models/execution.rs` - Scheduling-aware get_todays_tasks query
- `src-tauri/src/commands/task_commands.rs` - create_task/update_task commands with scheduling parameters
- `src-tauri/src/models/project.rs` - Updated test CreateTaskInput with new fields
- `src-tauri/src/models/tag.rs` - Updated test CreateTaskInput with new fields
- `src/lib/types.ts` - Task, CreateTaskInput, UpdateTaskInput with 5 scheduling fields
- `src/types/task.ts` - Re-exports from lib/types.ts, TaskDetail extends canonical Task
- `src/stores/taskSlice.ts` - updateTask parameter includes scheduling fields
- `src/components/center/TaskMetadata.tsx` - Fixed task.project -> task.projectId

## Decisions Made
- Used TASK_COLUMNS constant to centralize column list and prevent index drift across queries
- Consolidated dual Task type system: types/task.ts now re-exports from lib/types.ts instead of redefining
- Recurrence rule validation in Rust model layer, not SQL CHECK constraint, to avoid ALTER TABLE complications
- Option<String> where None means "don't change" -- matches existing codebase convention for update operations

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed task.project -> task.projectId in TaskMetadata.tsx**
- **Found during:** Task 2 (Frontend type consolidation)
- **Issue:** TaskMetadata.tsx referenced `task.project` which existed in the old divergent types/task.ts but not in the canonical lib/types.ts Task type
- **Fix:** Changed `task.project` to `task.projectId` to match the actual backend field name
- **Files modified:** src/components/center/TaskMetadata.tsx
- **Verification:** `npx tsc --noEmit` passes with 0 errors
- **Committed in:** 4c85b1b (Task 2 commit)

**2. [Rule 3 - Blocking] Updated CreateTaskInput in project.rs and tag.rs tests**
- **Found during:** Task 1 (Rust compilation)
- **Issue:** Tests in project.rs and tag.rs used CreateTaskInput without the new scheduling fields
- **Fix:** Added all 5 scheduling fields (set to None) to test CreateTaskInput usages
- **Files modified:** src-tauri/src/models/project.rs, src-tauri/src/models/tag.rs
- **Verification:** `cargo test` passes all 33 tests
- **Committed in:** 597853e (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (1 bug, 1 blocking)
**Impact on plan:** Both necessary for compilation and correctness. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Scheduling fields are fully available in backend and frontend types
- get_todays_tasks returns scheduling-aware results ready for Today view consumption
- TypeScript types consolidated -- future plans can import from either path
- Ready for Plan 02 (global hotkey quick-capture) and Plan 03 (today view upgrade)

---
*Phase: 02.1-daily-ux-foundation*
*Completed: 2026-03-16*
