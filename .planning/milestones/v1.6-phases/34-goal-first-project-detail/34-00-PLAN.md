---
phase: 34-goal-first-project-detail
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/center/__tests__/GoalHeroCard.test.tsx
  - src/components/center/__tests__/WorkspaceButton.test.tsx
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-03]

must_haves:
  truths:
    - "Test stubs exist for GoalHeroCard before implementation begins"
    - "Test stubs exist for WorkspaceButton before implementation begins"
  artifacts:
    - path: "src/components/center/__tests__/GoalHeroCard.test.tsx"
      provides: "Test stubs for goal hero card rendering and inline editing"
      contains: "GoalHeroCard"
    - path: "src/components/center/__tests__/WorkspaceButton.test.tsx"
      provides: "Test stubs for workspace button dual-mode behavior"
      contains: "WorkspaceButton"
  key_links: []
---

<objective>
Create test stub files for GoalHeroCard (PROJ-01, PROJ-02) and WorkspaceButton (PROJ-03) before feature implementation begins in Plans 01 and 02.

Purpose: Nyquist compliance -- every feature task needs an automated verify target. These stubs provide the test files that Plans 01 and 02 will run against.
Output: Two test files with describe/it blocks that will initially fail (components don't exist yet) and pass once Plan 02 creates the implementations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-goal-first-project-detail/34-CONTEXT.md
@.planning/phases/34-goal-first-project-detail/34-UI-SPEC.md

<interfaces>
<!-- Existing test pattern from ProjectDetail.test.tsx -->
```typescript
import { describe, it, expect, vi } from "vitest";

// Mock Tauri and dialog to avoid runtime dependency
vi.mock("@tauri-apps/plugin-dialog", () => ({ open: vi.fn() }));
vi.mock("@tauri-apps/api/core", () => ({ invoke: vi.fn() }));
```

<!-- GoalHeroCard props (will be created in Plan 02) -->
```typescript
interface GoalHeroCardProps {
  projectId: string;
  goal: string;
}
```

<!-- WorkspaceButton props (will be created in Plan 02) -->
```typescript
interface WorkspaceButtonProps {
  projectId: string;
  directoryPath: string | null;
  onLink: (path: string) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for GoalHeroCard and WorkspaceButton</name>
  <files>
    src/components/center/__tests__/GoalHeroCard.test.tsx,
    src/components/center/__tests__/WorkspaceButton.test.tsx
  </files>
  <read_first>
    src/components/center/__tests__/ProjectDetail.test.tsx
  </read_first>
  <action>
**Create `src/components/center/__tests__/GoalHeroCard.test.tsx`:**

Follow the same mock pattern as ProjectDetail.test.tsx. Import from vitest and @testing-library/react.

```typescript
import { describe, it, expect, vi } from "vitest";
import { render, screen, fireEvent, waitFor } from "@testing-library/react";

// Mock Tauri to avoid runtime dependency
vi.mock("@tauri-apps/api/core", () => ({ invoke: vi.fn() }));

// Mock the tauri API module
const mockUpdateProjectGoal = vi.fn().mockResolvedValue({});
const mockLoadProjects = vi.fn();
vi.mock("@/lib/tauri", () => ({
  api: { updateProjectGoal: (...args: unknown[]) => mockUpdateProjectGoal(...args) },
}));
vi.mock("@/stores", () => ({
  useStore: Object.assign(vi.fn(), {
    getState: () => ({ loadProjects: mockLoadProjects }),
  }),
}));
```

Test cases for PROJ-01 (goal display):
- `it("renders goal text when goal is provided")` -- render with `goal="Ship v2 by Q3"`, expect text visible
- `it("renders Target icon")` -- check for the icon element via test-id or role
- `it("renders empty state when goal is empty string")` -- render with `goal=""`, expect "Set a project goal..." text
- `it("has region role with project goal aria-label")` -- check `role="region"` and `aria-label="Project goal"`

Test cases for PROJ-02 (goal editing):
- `it("shows edit pencil button with aria-label")` -- find button with `aria-label="Edit goal"`
- `it("enters edit mode when pencil is clicked")` -- click pencil, expect input to appear with `aria-label="Project goal"`
- `it("enters edit mode when empty card is clicked")` -- render empty goal, click card, expect input
- `it("reverts on Escape key")` -- enter edit, type new text, press Escape, expect original goal displayed
- `it("calls updateProjectGoal on blur after debounce")` -- enter edit, change value, blur, advance timers by 800ms, expect mockUpdateProjectGoal called with correct args

Use `vi.useFakeTimers()` and `vi.advanceTimersByTime(800)` for debounce tests. Call `vi.useRealTimers()` in afterEach.

---

**Create `src/components/center/__tests__/WorkspaceButton.test.tsx`:**

```typescript
import { describe, it, expect, vi } from "vitest";
import { render, screen, fireEvent } from "@testing-library/react";

vi.mock("@tauri-apps/api/core", () => ({ invoke: vi.fn() }));
vi.mock("@tauri-apps/plugin-dialog", () => ({ open: vi.fn() }));

const mockStartFileWatcher = vi.fn().mockResolvedValue(undefined);
const mockOpenTerminal = vi.fn();
const mockSetProjectCenterTab = vi.fn();
vi.mock("@/lib/tauri", () => ({
  api: { startFileWatcher: (...args: unknown[]) => mockStartFileWatcher(...args) },
}));
vi.mock("@/stores/useWorkspaceStore", () => ({
  useWorkspaceStore: Object.assign(vi.fn(() => mockOpenTerminal), {
    getState: () => ({
      openTerminal: mockOpenTerminal,
      setProjectCenterTab: mockSetProjectCenterTab,
    }),
  }),
}));
```

Test cases for PROJ-03:
- `it("renders 'Open Workspace' when directoryPath is set")` -- render with `directoryPath="/path/to/dir"`, expect "Open Workspace" text
- `it("renders 'Link Directory' when directoryPath is null")` -- render with `directoryPath={null}`, expect "Link Directory" text
- `it("displays directory path as label")` -- render with directoryPath, expect path text visible
- `it("calls startFileWatcher, setProjectCenterTab, and openTerminal on workspace click")` -- click "Open Workspace", expect all three mocks called
- `it("calls onLink when directory is selected via dialog")` -- mock dialog open to return a path string, click "Link Directory", expect onLink called with path

These tests will initially fail because the components don't exist yet. That's expected -- Plan 02 Task 1 creates the implementations that make them pass.

**Important:** Write the import for the component but comment it out with a note:
```typescript
// Uncomment when component is created in Plan 02:
// import { GoalHeroCard } from "../GoalHeroCard";
```

Actually, do NOT comment out the import. Write the full import. The tests are meant to fail at this stage (RED phase of test-first). Plan 02's executor will see the tests, create the components, and make them pass.
  </action>
  <verify>
    <automated>cd /Users/cuhnowts/projects/element && ls -la src/components/center/__tests__/GoalHeroCard.test.tsx src/components/center/__tests__/WorkspaceButton.test.tsx 2>&1</automated>
  </verify>
  <done>GoalHeroCard.test.tsx exists with 9 test cases covering PROJ-01 (display, empty state, a11y) and PROJ-02 (edit mode, debounce save, escape revert). WorkspaceButton.test.tsx exists with 5 test cases covering PROJ-03 (dual mode, workspace open, directory link).</done>
</task>

</tasks>

<verification>
- `ls src/components/center/__tests__/GoalHeroCard.test.tsx` exists
- `ls src/components/center/__tests__/WorkspaceButton.test.tsx` exists
- Both files contain `describe` blocks with `it` test cases
- GoalHeroCard tests reference `updateProjectGoal`, `"Set a project goal..."`, `aria-label="Edit goal"`
- WorkspaceButton tests reference `"Open Workspace"`, `"Link Directory"`, `startFileWatcher`
</verification>

<success_criteria>
- Test stub files exist and are syntactically valid TypeScript
- Tests cover all three PROJ requirements
- Plan 02 can point its verify commands at these test files
</success_criteria>

<output>
After completion, create `.planning/phases/34-goal-first-project-detail/34-00-SUMMARY.md`
</output>
