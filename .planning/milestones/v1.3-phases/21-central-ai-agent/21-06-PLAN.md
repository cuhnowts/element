---
phase: 21-central-ai-agent
plan: 06
type: execute
wave: 4
depends_on:
  - 21-04
  - 21-05
files_modified:
  - mcp-server/src/__tests__/tool-registry.test.ts
  - mcp-server/src/__tests__/project-tools.test.ts
  - src/components/agent/__tests__/AgentPanel.test.tsx
autonomous: false
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - AGENT-06
must_haves:
  truths:
    - "MCP server tool registry test confirms all 10 tools are registered"
    - "MCP server project-tools test confirms list_projects returns data from SQLite"
    - "Agent panel test confirms rendering with Activity and Terminal sub-tabs"
    - "End-to-end flow is visually verified by user: agent starts, panel opens, activity tab shows"
  artifacts:
    - path: "mcp-server/src/__tests__/tool-registry.test.ts"
      provides: "Test that all MCP tools are registered with correct schemas"
    - path: "mcp-server/src/__tests__/project-tools.test.ts"
      provides: "Test that project read tools return correct data"
    - path: "src/components/agent/__tests__/AgentPanel.test.tsx"
      provides: "Test that agent panel renders and sub-tabs switch"
  key_links:
    - from: "mcp-server/src/__tests__/tool-registry.test.ts"
      to: "mcp-server/src/index.ts"
      via: "Tests tool listing matches expected tools"
      pattern: "list_projects.*get_project_detail.*request_approval"
---

<objective>
Add integration tests for the MCP server and agent panel, then run a user verification checkpoint to confirm the full agent experience works end-to-end.

Purpose: The MCP server and agent UI are the two critical systems. Tests ensure tool registration and data access work. The checkpoint verifies the visual experience and agent lifecycle.

Output: Test files for MCP server and agent panel, user-verified agent experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-central-ai-agent/21-CONTEXT.md
@.planning/phases/21-central-ai-agent/21-01-SUMMARY.md
@.planning/phases/21-central-ai-agent/21-02-SUMMARY.md
@.planning/phases/21-central-ai-agent/21-03-SUMMARY.md
@.planning/phases/21-central-ai-agent/21-04-SUMMARY.md
@.planning/phases/21-central-ai-agent/21-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write MCP server and agent panel tests</name>
  <files>mcp-server/src/__tests__/tool-registry.test.ts, mcp-server/src/__tests__/project-tools.test.ts, src/components/agent/__tests__/AgentPanel.test.tsx</files>
  <read_first>
    - mcp-server/src/index.ts (tool registration to test)
    - mcp-server/src/tools/project-tools.ts (project tool handlers to test)
    - mcp-server/src/db.ts (DB setup to mock)
    - mcp-server/package.json (check if vitest is configured, may need to add)
    - src/components/agent/AgentPanel.tsx (component to test)
    - src/stores/useAgentStore.ts (store to mock/use in tests)
    - src/stores/useWorkspaceStore.test.ts (existing test patterns)
  </read_first>
  <action>
**MCP server test setup:**
The mcp-server sub-project needs its own test configuration. Add vitest to mcp-server/package.json devDependencies and a test script:
```json
"scripts": {
  "test": "vitest run",
  ...
},
"devDependencies": {
  "vitest": "^3.0.0",
  ...
}
```

Create `mcp-server/vitest.config.ts`:
```typescript
import { defineConfig } from "vitest/config";
export default defineConfig({
  test: {
    globals: true,
  },
});
```

**mcp-server/src/__tests__/tool-registry.test.ts:**
Test that the MCP server registers all expected tools with correct schemas.

Since the server uses stdio transport and can't be easily instantiated in tests, test the tool listing handler directly:
1. Import the tool list definition (refactor: extract the tools array to a separate `tools/tool-definitions.ts` file if needed, or import the handler)
2. Verify all 10 tools are present: `list_projects`, `get_project_detail`, `list_phases`, `get_phase_status`, `list_tasks`, `request_approval`, `check_approval_status`, `send_notification`, `report_status`, `spawn_project_session`
3. For each tool, verify it has `name`, `description`, and `inputSchema`
4. Verify `request_approval` requires `projectId`, `projectName`, `phaseName`, `reason`
5. Verify `list_tasks` has optional `phaseId`

**mcp-server/src/__tests__/project-tools.test.ts:**
Test project read tools against an in-memory SQLite database.
1. Create a test SQLite database with `better-sqlite3` (in-memory: `new Database(":memory:")`)
2. Create the projects table with the same schema as Tauri's migration
3. Insert 2-3 test projects
4. Call `handleListProjects(testDb)` and verify it returns the test projects with correct fields
5. Call `handleGetProjectDetail(testDb, { projectId: "test-1" })` and verify it returns the correct project
6. Test not-found case for `handleGetProjectDetail`

**src/components/agent/__tests__/AgentPanel.test.tsx:**
Test the agent panel rendering and basic interactions.
1. Mock `useAgentStore` with test state
2. Mock `useTerminal` hook (return `{ isReady: true, error: null }`)
3. Test: Panel renders with "Agent" heading text
4. Test: Activity tab is selected by default (content visible)
5. Test: Clicking "Terminal" sub-tab switches content
6. Test: Empty state shows "No activity yet" text
7. Test: When entries exist, they render with correct titles
8. Test: Approval entry shows "Approve" and "Reject" buttons

After creating tests, run `cd mcp-server && npm install && npm test` and `npx vitest run src/components/agent/__tests__/AgentPanel.test.tsx`.
  </action>
  <verify>
    <automated>cd /Users/cuhnowts/projects/element && cd mcp-server && npm test 2>&1 | tail -10; cd .. && npx vitest run src/components/agent/__tests__/AgentPanel.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>MCP server has tool registry and project-tools tests passing. Agent panel has rendering tests passing. All tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify agent end-to-end experience</name>
  <what-built>
Complete central AI agent system:
- MCP server sidecar with 10 tools (project reads + orchestration)
- Agent Zustand store with activity log and approval flow
- Agent lifecycle management (auto-start, backoff restart)
- Agent sidebar panel with Activity + Terminal sub-tabs
- Agent toggle button with approval badge count
- Bidirectional file-based queue for MCP server communication
- OpenAiButton delegation to agent when running
  </what-built>
  <how-to-verify>
1. Start the app with `npm run tauri dev`
2. Verify the agent toggle button (Bot icon) appears in the drawer handle bar at the far right
3. Click the toggle button -- agent panel should slide in from right at 320px width
4. Verify panel header shows "Agent" with a status dot (amber "Starting..." or green "Running" depending on CLI config)
5. Verify Activity tab is selected by default and shows "No activity yet" empty state
6. Click "Terminal" sub-tab -- should show the agent terminal output (or empty state if CLI not configured)
7. Click "Activity" sub-tab -- should switch back
8. Try Cmd+Shift+A keyboard shortcut -- should toggle the panel
9. If CLI tool is configured in Settings > AI:
   - Agent should auto-start and show green "Running" status
   - Open a project and click "Open AI" -- should see toast "Session requested -- agent is setting up context" instead of direct terminal spawn
10. If CLI tool is NOT configured:
    - Agent should show "Stopped" status with "Restart Agent" button
    - Clicking "Open AI" on a project should fall back to direct terminal spawn (existing behavior)
11. Close the agent panel -- center content should reclaim the full width
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues you see</resume-signal>
</task>

</tasks>

<verification>
1. All MCP server tests pass
2. All agent panel tests pass
3. Full app test suite passes: `npm run test`
4. User confirms visual and functional experience matches expectations
</verification>

<success_criteria>
- MCP server registers all 10 tools (tested)
- Project read tools query correct SQLite tables (tested)
- Agent panel renders with correct layout and interactions (tested)
- User confirms end-to-end experience works in running app
</success_criteria>

<output>
After completion, create `.planning/phases/21-central-ai-agent/21-06-SUMMARY.md`
</output>
