---
phase: 02-task-ui-and-execution-history
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/AppLayout.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
gap_closure: true

must_haves:
  truths:
    - "App renders Phase 2 multi-panel layout: 280px sidebar, center panel, collapsible output drawer"
    - "Welcome dashboard with time-based greeting shown when no task is selected"
    - "Calendar toggle switch shows/hides mini calendar in sidebar"
    - "Sidebar shows Today's Tasks section with status dots and task items"
  artifacts:
    - path: "src/components/layout/AppLayout.tsx"
      provides: "Phase 2 layout shell importing Sidebar, CenterPanel, OutputDrawer"
      contains: "useWorkspaceStore"
  key_links:
    - from: "src/components/layout/AppLayout.tsx"
      to: "src/components/layout/Sidebar.tsx"
      via: "direct import and render"
      pattern: "import.*Sidebar.*from"
    - from: "src/components/layout/AppLayout.tsx"
      to: "src/components/layout/CenterPanel.tsx"
      via: "direct import and render inside ResizablePanel"
      pattern: "import.*CenterPanel.*from"
    - from: "src/components/layout/AppLayout.tsx"
      to: "src/components/layout/OutputDrawer.tsx"
      via: "direct import and render inside collapsible ResizablePanel"
      pattern: "import.*OutputDrawer.*from"
    - from: "src/components/layout/AppLayout.tsx"
      to: "src/stores/useWorkspaceStore.ts"
      via: "useWorkspaceStore hook for drawer/calendar/task state"
      pattern: "useWorkspaceStore"
---

<objective>
Restore AppLayout.tsx to render the Phase 2 multi-panel layout instead of the Phase 1 layout that overwrote it.

Purpose: Phase 1 Plan 03 (commit b81a5d5) overwrote AppLayout.tsx with Phase 1 components (ProjectList, NewTaskList, old TaskDetail/EmptyState, dialogs). This broke all Phase 2 UI because the Phase 2 layout components (Sidebar, CenterPanel, OutputDrawer) are never mounted. All three UAT failures (tests 2, 3, 4) and all four skipped tests (5, 6, 7, 8) trace to this single root cause.

Output: AppLayout.tsx restored to Phase 2 layout with Phase 1 dialogs preserved.
</objective>

<execution_context>
@/Users/cuhnowts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cuhnowts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-task-ui-and-execution-history/02-UAT.md
@.planning/phases/02-task-ui-and-execution-history/02-01-SUMMARY.md
@.planning/phases/02-task-ui-and-execution-history/02-03-SUMMARY.md

<interfaces>
<!-- Phase 2 layout components that must be imported and rendered -->

From src/components/layout/Sidebar.tsx:
```typescript
export function Sidebar()
// Renders: CalendarToggle, MiniCalendar (conditional), TaskList, WorkflowList
// Uses: useWorkspaceStore for calendarVisible
```

From src/components/layout/CenterPanel.tsx:
```typescript
export function CenterPanel()
// Renders: WelcomeDashboard (no selection) or TaskDetail (task selected)
// Uses: useWorkspaceStore for selectedTaskId
```

From src/components/layout/OutputDrawer.tsx:
```typescript
export function OutputDrawer()
// Renders: DrawerHeader + LogViewer
// Uses: useTaskStore for executionLogs
```

From src/stores/useWorkspaceStore.ts:
```typescript
export const useWorkspaceStore: {
  drawerOpen: boolean;
  drawerHeight: number;      // percentage 0-100
  calendarVisible: boolean;
  selectedTaskId: string | null;
  toggleDrawer: () => void;
  selectTask: (id: string | null) => void;
  setDrawerHeight: (height: number) => void;
}
```

From src/stores/index.ts (Phase 1 store — needed for dialogs):
```typescript
export const useStore: AppStore  // ProjectSlice & TaskSlice & UiSlice
// UiSlice provides: createProjectDialogOpen, deleteConfirmOpen, deleteTarget, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore Phase 2 layout in AppLayout.tsx with Phase 1 dialogs</name>
  <files>src/components/layout/AppLayout.tsx</files>
  <read_first>
    src/components/layout/AppLayout.tsx,
    src/components/layout/Sidebar.tsx,
    src/components/layout/CenterPanel.tsx,
    src/components/layout/OutputDrawer.tsx,
    src/stores/useWorkspaceStore.ts,
    src/stores/index.ts
  </read_first>
  <action>
Rewrite src/components/layout/AppLayout.tsx to restore the Phase 2 multi-panel layout while keeping the Phase 1 dialogs (Create Project, Delete Confirmation).

The component must:

1. **Import Phase 2 layout components:** Sidebar, CenterPanel, OutputDrawer from "@/components/layout/*"
2. **Import useWorkspaceStore** (not useStore) for drawer state, task selection, and keyboard shortcuts
3. **Import useStore** (Phase 1 store) ONLY for the dialog state (createProjectDialogOpen, deleteConfirmOpen, etc.)
4. **Import ResizablePanelGroup, ResizablePanel, ResizableHandle** from "@/components/ui/resizable"
5. **Import Dialog components** from "@/components/ui/dialog" (for Create Project and Delete dialogs)
6. **Import Input, Textarea, Button, Separator** for the dialog forms

Layout structure (matching the original Phase 2 design from 02-01-PLAN):
- Outer: `<div className="flex h-screen bg-background text-foreground">`
- Left: `<aside className="w-[280px] border-r border-border flex-shrink-0 overflow-hidden"><Sidebar /></aside>`
- Right: `<ResizablePanelGroup direction="vertical" className="flex-1">` containing:
  - Top panel: `<ResizablePanel>` with `<CenterPanel />`
  - `<ResizableHandle withHandle />`
  - Bottom panel: `<ResizablePanel collapsible collapsedSize={0}>` with `<OutputDrawer />`

The drawer panel defaultSize should be `drawerOpen ? drawerHeight : 0` (from useWorkspaceStore), minSize={0}, maxSize={60}. The center panel defaultSize should be `drawerOpen ? `${100 - drawerHeight}%`` : `"100%"`, minSize={30}.

Use onLayout callback on ResizablePanelGroup to call setDrawerHeight when sizes change (only update if sizes[1] is defined).

After the layout div, include the EXISTING Create Project Dialog and Delete Confirmation Dialog JSX from the current file verbatim (these use useStore from Phase 1 and must be preserved for Phase 1 functionality to keep working). Keep the useState hooks for newProjectName and newProjectDescription, and the handleCreateProject/handleDeleteConfirm functions.

Do NOT import or render ProjectList, NewTaskList, or the old Phase 1 TaskDetail/EmptyState components — those are replaced by the Phase 2 Sidebar and CenterPanel.

Note: Do NOT add useKeyboardShortcuts here — App.tsx already calls it at the top level.
  </action>
  <verify>
    <automated>
      grep -q "import.*Sidebar.*from" src/components/layout/AppLayout.tsx &&
      grep -q "import.*CenterPanel.*from" src/components/layout/AppLayout.tsx &&
      grep -q "import.*OutputDrawer.*from" src/components/layout/AppLayout.tsx &&
      grep -q "useWorkspaceStore" src/components/layout/AppLayout.tsx &&
      grep -q 'w-\[280px\]' src/components/layout/AppLayout.tsx &&
      grep -q 'direction="vertical"' src/components/layout/AppLayout.tsx &&
      grep -q "collapsible" src/components/layout/AppLayout.tsx &&
      grep -q "createProjectDialogOpen" src/components/layout/AppLayout.tsx &&
      grep -q "deleteConfirmOpen" src/components/layout/AppLayout.tsx &&
      ! grep -q "ProjectList" src/components/layout/AppLayout.tsx &&
      ! grep -q "NewTaskList" src/components/layout/AppLayout.tsx &&
      echo "PASS: AppLayout restored to Phase 2 layout with Phase 1 dialogs" &&
      npx tsc --noEmit &&
      echo "PASS: TypeScript compiles cleanly"
    </automated>
  </verify>
  <done>
    - AppLayout renders Phase 2 Sidebar (with CalendarToggle, TaskList) in 280px aside
    - AppLayout renders Phase 2 CenterPanel (WelcomeDashboard or TaskDetail) in center
    - AppLayout renders Phase 2 OutputDrawer (DrawerHeader + LogViewer) in collapsible bottom panel
    - Phase 1 Create Project and Delete Confirmation dialogs still functional
    - No Phase 1 layout components (ProjectList, NewTaskList) rendered
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. App launches and shows Phase 2 three-panel layout (sidebar + center + bottom drawer)
2. Welcome dashboard with time-based greeting appears when no task selected
3. Sidebar shows Calendar toggle and Today's Tasks section
4. Output drawer at bottom with Show/Hide toggle
5. Create Project dialog still works (from Phase 1)
6. Delete Confirmation dialog still works (from Phase 1)
</verification>

<success_criteria>
- All three UAT gap truths are resolved by mounting the Phase 2 layout components
- Previously skipped UAT tests (5, 6, 7, 8) are unblocked
- Phase 1 dialog functionality is preserved
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-task-ui-and-execution-history/02-04-SUMMARY.md`
</output>
