---
phase: 26
slug: calendar-sync-foundation
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-04-03
revised: 2026-04-03
---

# Phase 26 -- UI Design Contract

> Visual and interaction contract for Phase 26: Calendar Sync Foundation.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Scope note:** This phase is infrastructure/reliability work. There are no new UI surfaces -- only modifications to the existing CalendarAccounts settings component and a warning badge on the settings nav item. All visual tokens below are inherited from the established design system; they are restated here for executor reference.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | base-nova |
| Component library | radix (via shadcn) |
| Icon library | lucide |
| Font | Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing, badge offset |
| md | 16px | Default element spacing, card padding |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label | 12px | 600 | 1.33 |
| Heading | 16px | 600 | 1.25 |
| Display | 20px | 600 | 1.2 |

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `oklch(0.145 0 0)` / --background | Background, surfaces |
| Secondary (30%) | `oklch(0.205 0 0)` / --card | Cards, sidebar, settings panels |
| Accent (10%) | `oklch(0.269 0 0)` / --accent | Settings nav active tab, hover states |
| Destructive | `oklch(0.396 0.141 25.723)` / --destructive | Error banners, disconnect button |
| Warning | `oklch(0.828 0.189 84.429)` / --chart-4 (amber) | OAuth warning badge dot, reconnect prompt border |

Accent reserved for: settings nav active tab highlight, button hover backgrounds, focus rings.

Warning color note: The existing theme has no dedicated `--warning` token. Use `--chart-4` (amber, `oklch(0.828 0.189 84.429)`) for the warning badge and reconnect prompt. This is consistent with the chart palette already used for calendar account color dots.

---

## Component Inventory

This phase modifies 2 existing components and adds 0 new component files. All changes are within the existing CalendarAccounts settings surface.

### Modified: SettingsNav (settings nav calendar tab)

**Change:** Add a warning badge dot when any connected calendar account has an OAuth failure state (`needs_reconnect`).

| Element | Specification |
|---------|---------------|
| Badge shape | 8px diameter circle, `bg-chart-4` (amber), positioned absolute top-right of the Calendar icon |
| Badge offset | `top: -4px`, `right: -4px` relative to the icon container |
| Badge visibility | Visible only when `calendarAccounts.some(a => a.needsReconnect)` is true |
| Accessibility | `aria-label` on the Calendars tab button must include "needs attention" when badge is visible |

### Modified: CalendarAccounts (settings page)

**Change 1: Reconnect prompt per account**

When an account enters `needs_reconnect` state (invalid_grant / revoked access):

| Element | Specification |
|---------|---------------|
| Account row border | Change from `border-border` to `border-chart-4/50` (amber, 50% opacity) |
| Account row background | `bg-chart-4/5` (amber, 5% opacity) |
| Status text | Replace "Last synced {time}" with "Connection expired" in `text-chart-4` |
| Reconnect button | `<Button variant="outline" size="sm">` with label "Reconnect Account" placed inline where sync/disconnect buttons are. Uses default outline variant, not destructive. |
| Sync button | Hidden when `needs_reconnect` is true (reconnect replaces it) |
| Disconnect button | Remains visible alongside Reconnect Account |

**Focal point -- error recovery flow:** When an account row enters the `needs_reconnect` state, the amber border + amber background + "Connection expired" status text + inline "Reconnect Account" button form the primary visual anchor for the error recovery flow. The amber highlight draws the eye to the affected row; the button provides the single corrective action.

**Change 2: Placeholder OAuth client ID error**

When user clicks Connect Google/Outlook and the backend detects placeholder client IDs:

| Element | Specification |
|---------|---------------|
| Error container | Same destructive error banner pattern as existing `calendarError` display: `rounded-md border border-destructive/50 bg-destructive/10 px-3 py-2 text-sm text-destructive-foreground` |
| Error icon | `AlertCircle` from lucide, `size-4 shrink-0` |
| Error copy | See Copywriting Contract below |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Reconnect Account" (on accounts with expired OAuth tokens) |
| Empty state heading | "No calendars connected" (existing, unchanged) |
| Empty state body | "Connect a Google or Outlook calendar to see your events in Element." (existing, unchanged) |
| Error: placeholder OAuth (Google) | "Google Calendar is not configured. Set the GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET environment variables with credentials from the Google Cloud Console, then restart Element." |
| Error: placeholder OAuth (Outlook) | "Outlook Calendar is not configured. Set the OUTLOOK_CLIENT_ID and OUTLOOK_CLIENT_SECRET environment variables with credentials from the Microsoft Azure portal, then restart Element." |
| Error: connection expired | "Connection expired" (inline status replacing "Last synced" timestamp) |
| Error: sync failure (transient) | No visible copy -- transient failures are silent per D-06 |
| Disconnect confirmation heading | "Disconnect Calendar" (existing, unchanged) |
| Disconnect confirmation body | "This will remove the connection to {email}. Calendar events from this account will no longer appear in Element." (existing, unchanged) |
| Disconnect confirmation CTA | "Disconnect" (existing, unchanged) |

---

## Interaction States

### Account Row States

| State | Visual | Behavior |
|-------|--------|----------|
| Connected + synced | Default border, "Last synced {relative time}" | Hover shows sync/disconnect buttons |
| Connected + syncing | Default border, "Syncing..." with spin icon | Sync button disabled with spinner |
| Needs reconnect | Amber border, "Connection expired", Reconnect Account button visible | Clicking Reconnect Account initiates same OAuth flow as initial connect |
| Disconnecting | Confirmation dialog overlay | Cancel or confirm |

### Badge Lifecycle

| Trigger | Badge State |
|---------|-------------|
| Token refresh returns invalid_grant | Badge appears on Calendars nav tab |
| User clicks Reconnect Account and completes OAuth | Badge disappears once no accounts need reconnect |
| User disconnects the failing account | Badge disappears if no other accounts need reconnect |

### Background Sync (invisible)

| Event | User-visible effect |
|-------|-------------------|
| 15-minute timer fires | None (silent background operation) |
| App launch / Hub focus | None (sync happens silently) |
| Sync discovers token expired | Account row transitions to "needs reconnect" state; badge appears |
| Sync succeeds after error | Account row returns to normal; badge may disappear |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | badge (if not already installed) | not required |

Note: The warning dot is a simple CSS circle, not a full Badge component. No new shadcn components are strictly required for this phase. If the executor prefers using the shadcn Badge component for the dot, it is pre-approved from the official registry.

---

## Accessibility

| Requirement | Implementation |
|-------------|---------------|
| Warning badge | `aria-label` on Calendars tab: "Calendars (needs attention)" when badge is visible |
| Reconnect button | Standard button semantics, no additional ARIA needed |
| Error banners | Use `role="alert"` on placeholder OAuth error messages |
| Color-only indicators | Amber border on account row is paired with "Connection expired" text -- not color-only |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
