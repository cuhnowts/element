---
phase: 11-workspace-integration-and-ai-context
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/stores/useWorkspaceStore.test.ts
  - src/stores/contextSlice.test.ts
  - src/components/center/ContextSummaryCard.test.tsx
  - src/components/center/AiSuggestionCard.test.tsx
autonomous: true
requirements:
  - AIAS-02
  - AIAS-03

must_haves:
  truths:
    - "Test stubs exist for all behavioral requirements before implementation begins"
    - "All test files import vitest and follow project test conventions"
    - "Test suite runs green with all tests in todo/placeholder state"
  artifacts:
    - path: "src/stores/useWorkspaceStore.test.ts"
      provides: "Test stubs for per-project state, idle threshold, shouldShowSummary, recordProjectView"
      contains: "shouldShowSummary"
    - path: "src/stores/contextSlice.test.ts"
      provides: "Test stubs for generateContextSummary lifecycle, race condition guard, dismissal"
      contains: "generateContextSummary"
    - path: "src/components/center/ContextSummaryCard.test.tsx"
      provides: "Test stubs for render, loading, error, fallback states"
      contains: "ContextSummaryCard"
    - path: "src/components/center/AiSuggestionCard.test.tsx"
      provides: "Test stubs for render, action, dismiss, mode gating"
      contains: "AiSuggestionCard"
  key_links: []
---

<objective>
Wave 0: Create test stubs for all Phase 11 behavioral requirements before any implementation begins.

Purpose: Establishes the Nyquist verification contract. Every behavioral requirement from AIAS-02 and AIAS-03 gets a failing or todo test stub so that implementation tasks in Waves 1 and 2 can verify against specific test files rather than compiler-only checks.
Output: Four test files with descriptive test stubs covering workspace state, context slice lifecycle, and both UI components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-workspace-integration-and-ai-context/11-CONTEXT.md
@.planning/phases/11-workspace-integration-and-ai-context/11-RESEARCH.md
@.planning/phases/11-workspace-integration-and-ai-context/11-VALIDATION.md

<interfaces>
<!-- Existing test setup pattern from src/__tests__/setup.ts -->
```typescript
import { vi } from "vitest";
import "@testing-library/jest-dom";

vi.mock("@tauri-apps/api/core", () => ({
  invoke: vi.fn().mockResolvedValue(undefined),
}));
vi.mock("@tauri-apps/api/event", () => ({
  listen: vi.fn().mockResolvedValue(vi.fn()),
}));
```

<!-- Existing test pattern from src/stores/themeSlice.test.ts -->
```typescript
import { describe, it } from "vitest";

describe("themeSlice", () => {
  it.todo("loadThemes fetches themes from API and updates state");
});
```

<!-- Existing component test pattern from src/components/detail/AiSuggestionPanel.test.tsx -->
```typescript
import { describe, it, expect, vi } from "vitest";

vi.mock("@tauri-apps/api/core", () => ({
  invoke: vi.fn(),
}));

describe("AiSuggestionPanel", () => {
  it("renders nothing when no pending suggestions", () => {
    expect(true).toBe(true);
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create store test stubs (useWorkspaceStore + contextSlice)</name>
  <files>
    src/stores/useWorkspaceStore.test.ts,
    src/stores/contextSlice.test.ts
  </files>
  <action>
**1. Create `src/stores/useWorkspaceStore.test.ts`:**

Follow the existing `themeSlice.test.ts` pattern using `it.todo()` for stubs. Cover all AIAS-02 idle threshold and per-project state behaviors:

```typescript
import { describe, it } from "vitest";

describe("useWorkspaceStore — per-project state", () => {
  it.todo("getProjectState returns default state for unknown project ID");
  it.todo("setProjectCenterTab updates centerTab for specific project");
  it.todo("setProjectDrawer updates drawerOpen and drawerTab for specific project");
  it.todo("projectStates are independent — setting state for project A does not affect project B");
});

describe("useWorkspaceStore — idle threshold and summary trigger", () => {
  it.todo("shouldShowSummary returns true when project has no lastViewedTimestamp (first session view)");
  it.todo("shouldShowSummary returns false when project was viewed less than 30 minutes ago");
  it.todo("shouldShowSummary returns true when project was viewed 30+ minutes ago");
  it.todo("recordProjectView sets lastViewedTimestamp to current time");
  it.todo("recordProjectView updates existing timestamp for a project");
});

describe("useWorkspaceStore — persistence", () => {
  it.todo("projectStates are included in persisted state (partialize)");
  it.todo("lastViewedTimestamps are NOT included in persisted state (in-memory only per D-14)");
  it.todo("selectedTaskId is NOT included in persisted state");
});
```

**2. Create `src/stores/contextSlice.test.ts`:**

Cover AIAS-02 summary lifecycle and AIAS-03 suggestion lifecycle with race condition and dismissal behaviors:

```typescript
import { describe, it } from "vitest";

describe("contextSlice — summary lifecycle", () => {
  it.todo("generateContextSummary sets isGeneratingSummary true and clears previous summary");
  it.todo("generateContextSummary sets contextSummary on successful API response");
  it.todo("generateContextSummary sets summaryError on API failure");
  it.todo("generateContextSummary skips if project was already dismissed");
  it.todo("race condition guard: ignores response if currentRequestProjectId changed during request");
});

describe("contextSlice — suggestion lifecycle", () => {
  it.todo("generateSuggestions populates contextSuggestions on success");
  it.todo("generateSuggestions filters out previously dismissed suggestion IDs");
  it.todo("generateSuggestions sets empty array on failure (non-critical)");
  it.todo("race condition guard: ignores suggestions if currentRequestProjectId changed");
});

describe("contextSlice — dismissal", () => {
  it.todo("dismissSummary clears contextSummary and adds projectId to dismissedSummaryProjectIds");
  it.todo("dismissSuggestion removes suggestion from contextSuggestions and tracks in dismissedSuggestionIds");
  it.todo("dismissed summary projectId prevents re-generation for that project");
});

describe("contextSlice — clearContextState", () => {
  it.todo("clearContextState resets all context state to initial values");
});
```
  </action>
  <verify>
    <automated>cd /Users/cuhnowts/projects/element && npx vitest run src/stores/useWorkspaceStore.test.ts src/stores/contextSlice.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both store test files exist with descriptive todo stubs. `npx vitest run` passes (todo tests are skipped, not failures). All AIAS-02 idle threshold behaviors and AIAS-03 dismissal/race-condition behaviors have corresponding test stubs.</done>
</task>

<task type="auto">
  <name>Task 2: Create component test stubs (ContextSummaryCard + AiSuggestionCard)</name>
  <files>
    src/components/center/ContextSummaryCard.test.tsx,
    src/components/center/AiSuggestionCard.test.tsx
  </files>
  <action>
**1. Create `src/components/center/ContextSummaryCard.test.tsx`:**

Follow the existing `AiSuggestionPanel.test.tsx` pattern. Cover AIAS-02 render states:

```typescript
import { describe, it } from "vitest";

describe("ContextSummaryCard", () => {
  it.todo("renders loading skeleton with aria-busy when isLoading is true");
  it.todo("renders error state with retry button when error is set");
  it.todo("renders AI summary with 'Where was I?' header and Bot icon when isAiGenerated is true");
  it.todo("renders fallback template with 'Project Status' header and BarChart3 icon when isAiGenerated is false");
  it.todo("renders bullet list from summary.bullets");
  it.todo("renders summary.note as italic muted text when present");
  it.todo("calls onDismiss when dismiss button clicked on AI summary");
  it.todo("does not render dismiss button on fallback (non-AI) summary");
  it.todo("calls onRetry when retry button clicked in error state");
  it.todo("renders nothing when summary is null and not loading and no error");
});
```

**2. Create `src/components/center/AiSuggestionCard.test.tsx`:**

Cover AIAS-03 suggestion card behaviors:

```typescript
import { describe, it } from "vitest";

describe("AiSuggestionCard", () => {
  it.todo("renders suggestion text and action label");
  it.todo("renders Lightbulb icon");
  it.todo("calls onAction when action button clicked");
  it.todo("calls onDismiss when dismiss button clicked");
  it.todo("applies staggered animation delay based on index prop");
  it.todo("renders aria-label on dismiss button for accessibility");
});
```
  </action>
  <verify>
    <automated>cd /Users/cuhnowts/projects/element && npx vitest run src/components/center/ContextSummaryCard.test.tsx src/components/center/AiSuggestionCard.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both component test files exist with descriptive todo stubs covering all render states (loading, error, AI, fallback), interaction (dismiss, retry, action), and accessibility. `npx vitest run` passes with todo tests skipped.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/stores/useWorkspaceStore.test.ts src/stores/contextSlice.test.ts src/components/center/ContextSummaryCard.test.tsx src/components/center/AiSuggestionCard.test.tsx --reporter=verbose` -- all four files run with todo tests skipped, zero failures
- `grep -l "it.todo" src/stores/useWorkspaceStore.test.ts src/stores/contextSlice.test.ts src/components/center/ContextSummaryCard.test.tsx src/components/center/AiSuggestionCard.test.tsx` -- all four files contain test stubs
</verification>

<success_criteria>
- Four test files created with descriptive test stubs for all Phase 11 behavioral requirements
- Test stubs cover: idle threshold logic (AIAS-02), summary lifecycle with race guard (AIAS-02), suggestion lifecycle with dismissal (AIAS-03), all component render states, and accessibility
- `npx vitest run` passes (todo tests are skipped, not errors)
- Wave 1 and Wave 2 plans can reference these specific test files in their `<automated>` verify commands
</success_criteria>

<output>
After completion, create `.planning/phases/11-workspace-integration-and-ai-context/11-00-SUMMARY.md`
</output>
