import { describe, it } from "vitest";

describe("themeSlice", () => {
  it.todo("loadThemes fetches themes from API and updates state");
  it.todo("createTheme calls API and appends to themes array");
  it.todo("updateTheme calls API and replaces theme in array");
  it.todo("deleteTheme removes theme and nullifies project themeId references");
  it.todo("reorderThemes optimistically updates sortOrder before API call");
  it.todo("assignProjectToTheme calls API and updates project in store");
  it.todo("assignTaskToTheme calls API and updates task in store");
  it.todo("sets themesLoading true during loadThemes and false after");
});
